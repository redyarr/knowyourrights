<% // Define reaction icons for use in grouping and popup 
 let reactionTypes={ like: '👍' , love: '❤️' , haha: '😂' , wow: '😮' , sad: '😢' , angry: '😠' };
%>
<style>
  .modal-hidden {
    display: none !important;
  }
  
  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateX(-50%) translateY(20px) scale(0.8);
    }
    to {
      opacity: 1;
      transform: translateX(-50%) translateY(0) scale(1);
    }
  }
  
  @keyframes reactionBounce {
    0% { transform: scale(1) rotate(0deg); }
    25% { transform: scale(1.2) rotate(-5deg); }
    50% { transform: scale(1.1) rotate(5deg); }
    75% { transform: scale(1.15) rotate(-2deg); }
    100% { transform: scale(1) rotate(0deg); }
  }
  
  @keyframes floatUp {
    0% {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
    100% {
      opacity: 0;
      transform: translateY(-100px) scale(2) rotate(360deg);
    }
  }
  
  @keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
  }
  
  @keyframes shimmer {
    0% { background-position: -200% 0; }
    100% { background-position: 200% 0; }
  }
  
  .reaction-popup {
    animation: slideUp 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  
  .reaction-btn:hover {
    animation: reactionBounce 0.6s ease-in-out;
  }
  
  .like-button:active {
    animation: pulse 0.3s ease-in-out;
  }
  
  .floating-reaction {
    animation: floatUp 1s ease-out forwards;
  }
  
  .shimmer-effect {
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.4), transparent);
    background-size: 200% 100%;
    animation: shimmer 1.5s infinite;
  }
  
  .reaction-stats {
    transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  
  .reaction-icon {
    transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  
  .notification-enter {
    animation: slideInRight 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  
  @keyframes slideInRight {
    from {
      transform: translateX(100%) scale(0.8);
      opacity: 0;
    }
    to {
      transform: translateX(0) scale(1);
      opacity: 1;
    }
  }
</style>
    <div class="max-w-7xl mx-auto px-4 py-8">
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-8">
            <!-- Left Sidebar -->
            <div class="lg:col-span-3 space-y-8">
                <% if (locals.loggedInUserId) { %>
                    <div class="card bg-gradient-to-br from-blue-50 to-blue-100 shadow-2xl rounded-3xl overflow-hidden hover:shadow-3xl transition-all duration-500">
                        <div class="p-8 text-center">
                            <div class="relative inline-block mb-4">
                                <img src="https://media.licdn.com/dms/image/v2/D4D03AQF9FvdZ1W1_9g/profile-displayphoto-shrink_400_400/profile-displayphoto-shrink_400_400/0/1712227644410?e=1746662400&v=beta&t=h_guvHTPpOuEEWiAAnuGwQLiaaMLVRgSJGwzP79vRlU" alt="Profile" class="profile-img-lg mx-auto" style="width: 100px; height: 100px;">
                                <div class="absolute -bottom-2 -right-2 w-8 h-8 bg-green-500 rounded-full border-4 border-white flex items-center justify-center">
                                    <i class="fas fa-check text-white text-xs"></i>
                                </div>
                            </div>
                            <h5 class="font-bold text-xl text-gray-900 mb-2">Your Profile</h5>
                            <p class="text-sm text-blue-700 mb-4 font-medium">Manage your professional presence</p>
                            <a href="/in" class="btn btn-primary inline-flex items-center gap-2">
                                <i class="fas fa-user"></i>
                                View Profile
                            </a>
                        </div>
                    </div>
                <% } %>
                <div class="card bg-white rounded-2xl shadow-lg">
                    <div class="border-b border-gray-100 px-6 py-5">
                        <h6 class="font-bold text-gray-800 text-lg flex items-center gap-2">
                            <i class="fas fa-book-open text-blue-600"></i>
                            Legal Resource Guides
                        </h6>
                    </div>
                    <div class="p-6">
                        <ul class="space-y-3">
                            <li>
                                <a href="/resources/family-law" class="flex items-center p-3 rounded-xl hover:bg-blue-50 transition-all duration-300 group">
                                    <div class="w-10 h-10 bg-blue-100 rounded-lg flex items-center justify-center mr-4 group-hover:bg-blue-200 transition-colors">
                                        <i class="fas fa-home text-blue-600"></i>
                                    </div>
                                    <div>
                                        <span class="text-gray-800 font-semibold group-hover:text-blue-600 transition-colors">Family Law Guide</span>
                                        <p class="text-sm text-gray-500">Divorce, custody, and more</p>
                                    </div>
                                </a>
                            </li>
                            <li>
                                <a href="/resources/employment" class="flex items-center p-3 rounded-xl hover:bg-green-50 transition-all duration-300 group">
                                    <div class="w-10 h-10 bg-green-100 rounded-lg flex items-center justify-center mr-4 group-hover:bg-green-200 transition-colors">
                                        <i class="fas fa-briefcase text-green-600"></i>
                                    </div>
                                    <div>
                                        <span class="text-gray-800 font-semibold group-hover:text-green-600 transition-colors">Employment Rights</span>
                                        <p class="text-sm text-gray-500">Workplace protection</p>
                                    </div>
                                </a>
                            </li>
                            <li>
                                <a href="/resources/consumer" class="flex items-center p-3 rounded-xl hover:bg-purple-50 transition-all duration-300 group">
                                    <div class="w-10 h-10 bg-purple-100 rounded-lg flex items-center justify-center mr-4 group-hover:bg-purple-200 transition-colors">
                                        <i class="fas fa-shield-alt text-purple-600"></i>
                                    </div>
                                    <div>
                                        <span class="text-gray-800 font-semibold group-hover:text-purple-600 transition-colors">Consumer Protection</span>
                                        <p class="text-sm text-gray-500">Rights and remedies</p>
                                    </div>
                                </a>
                            </li>
                            <li>
                                <a href="/search" class="flex items-center p-3 rounded-xl hover:bg-indigo-50 transition-all duration-300 group">
                                    <div class="w-10 h-10 bg-indigo-100 rounded-lg flex items-center justify-center mr-4 group-hover:bg-indigo-200 transition-colors">
                                        <i class="fas fa-search text-indigo-600"></i>
                                    </div>
                                    <div>
                                        <span class="text-gray-800 font-semibold group-hover:text-indigo-600 transition-colors">Find Legal Help</span>
                                        <p class="text-sm text-gray-500">Locate attorneys</p>
                                    </div>
                                </a>
                            </li>
                            <li>
                                <a href="/mynetwork" class="flex items-center p-3 rounded-xl hover:bg-orange-50 transition-all duration-300 group">
                                    <div class="w-10 h-10 bg-orange-100 rounded-lg flex items-center justify-center mr-4 group-hover:bg-orange-200 transition-colors">
                                        <i class="fas fa-users text-orange-600"></i>
                                    </div>
                                    <div>
                                        <span class="text-gray-800 font-semibold group-hover:text-orange-600 transition-colors">Connect with Lawyers</span>
                                        <p class="text-sm text-gray-500">Build your network</p>
                                    </div>
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>

            <!-- Main Content -->
            <div class="lg:col-span-6 space-y-8">
                <% if (locals.loggedInUserId && locals.userRole === 'lawyer') { %>
                    <div class="bg-white rounded-2xl shadow-lg mb-4">
                        <div class="p-6 flex items-center space-x-4">
                            <img src="https://media.licdn.com/dms/image/v2/D4D03AQF9FvdZ1W1_9g/profile-displayphoto-shrink_400_400/profile-displayphoto-shrink_400_400/0/1712227644410?e=1746662400&v=beta&t=h_guvHTPpOuEEWiAAnuGwQLiaaMLVRgSJGwzP79vRlU" alt="Profile" class="rounded-full shadow border-2 border-white" style="width: 48px; height: 48px; object-fit: cover;">
                            <button onclick="togglePostModal()" class="flex-1 border border-blue-200 rounded-full px-6 py-3 text-md text-gray-700 bg-blue-50 hover:bg-blue-100 shadow">Share Legal Insights</button>
                        </div>
                    </div>
                <% } else if (locals.loggedInUserId && locals.userRole !== 'lawyer') { %>
                    <div class="bg-white rounded-2xl shadow-lg mb-4">
                        <div class="p-8">
                            <div class="flex items-center space-x-4 mb-4">
                                <img src="https://media.licdn.com/dms/image/v2/D4D03AQF9FvdZ1W1_9g/profile-displayphoto-shrink_400_400/profile-displayphoto-shrink_400_400/0/1712227644410?e=1746662400&v=beta&t=h_guvHTPpOuEEWiAAnuGwQLiaaMLVRgSJGwzP79vRlU" alt="Profile" class="rounded-full shadow border-2 border-white" style="width: 48px; height: 48px; object-fit: cover;">
                                <div class="flex-1">
                                    <h3 class="text-lg font-semibold text-gray-800">Welcome to the Legal Community!</h3>
                                    <p class="text-gray-600">Connect with lawyers, ask questions, and explore job opportunities.</p>
                                </div>
                            </div>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                                <a href="/jobs" class="flex items-center justify-center px-4 py-3 bg-green-50 border border-green-200 rounded-lg hover:bg-green-100 transition">
                                    <i class="fas fa-briefcase text-green-600 mr-2"></i>
                                    <span class="text-green-700 font-medium">Post Job Opportunity</span>
                                </a>
                                <a href="/mynetwork" class="flex items-center justify-center px-4 py-3 bg-blue-50 border border-blue-200 rounded-lg hover:bg-blue-100 transition">
                                    <i class="fas fa-users text-blue-600 mr-2"></i>
                                    <span class="text-blue-700 font-medium">Find Lawyers</span>
                                </a>
                            </div>
                        </div>
                    </div>
                <% } %>
                <% if (posts && posts.length > 0) { %>
                    <% posts.forEach(post => { let groupedReactions = {}; if (post.reacts && post.reacts.length > 0) { post.reacts.forEach(r => { groupedReactions[r.reaction] = (groupedReactions[r.reaction] || 0) + 1; }); } %>
                    <div id="post-<%= post.id %>" class="bg-white/95 backdrop-blur-lg rounded-3xl shadow-2xl mb-10 border border-white/20 hover:shadow-3xl transition-all duration-500 transform hover:-translate-y-2 overflow-visible">
                        <div class="p-8">
                            <!-- Enhanced Post Header -->
                            <div class="flex items-center mb-6">
                                <a href="/in/<%= post.user ? post.user.id : '#' %>" class="flex items-center hover:opacity-80 transition-all duration-300 transform hover:scale-105">
                                    <div class="relative">
                                        <img src="https://ui-avatars.com/api/?name=<%= post.user ? encodeURIComponent(post.user.firstName + ' ' + post.user.lastName) : 'User' %>&background=0D47A1&color=fff&size=64" alt="Profile" class="rounded-full mr-5 shadow-2xl ring-4 ring-white/50" style="width: 64px; height: 64px; object-fit: cover;">
                                        <div class="absolute bottom-0 right-3 w-4 h-4 bg-green-500 rounded-full border-2 border-white shadow-lg animate-pulse"></div>
                                    </div>
                                    <div class="flex-1">
                                        <div class="flex items-center space-x-2 mb-1">
                                            <h6 class="font-bold text-xl text-gray-900 hover:text-blue-600 transition-colors">
                                                <%= post.user ? post.user.firstName : '' %> <%= post.user ? post.user.lastName : '' %>
                                            </h6>
                                            <% if (post.user && post.user.role==='lawyer' && post.user.lawyer) { %>
                                                <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-bold bg-gradient-to-r from-blue-500 to-indigo-600 text-white shadow-lg">
                                                    <i class="fas fa-gavel text-xs mr-1"></i>
                                                    Verified Lawyer
                                                </span>
                                                <% 
                                                    // Get lawyer rank information
                                                    let rankInfo = {
                                                        badge: 'Training',
                                                        color: 'yellow',
                                                        icon: '🎓'
                                                    };
                                                    
                                                    if (post.user.lawyer.badgeIssuingAuthority === 'approved') {
                                                        rankInfo = {
                                                            badge: 'Approved',
                                                            color: 'green',
                                                            icon: '✓'
                                                        };
                                                    } else if (post.user.lawyer.badgeIssuingAuthority === 'consultant') {
                                                        rankInfo = {
                                                            badge: 'Consultant',
                                                            color: 'purple',
                                                            icon: '⭐'
                                                        };
                                                    }
                                                %>
                                                <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-<%= rankInfo.color %>-100 text-<%= rankInfo.color %>-800 border border-<%= rankInfo.color %>-200">
                                                    <span class="mr-1"><%= rankInfo.icon %></span>
                                                    <%= rankInfo.badge %>
                                                </span>
                                            <% } else { %>
                                                <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-gray-100 text-gray-600">
                                                    <i class="fas fa-user text-xs mr-1"></i>
                                                    Member
                                                </span>
                                            <% } %>
                                        </div>
                                        <div class="flex items-center space-x-3">
                                            <% if (post.user && post.user.role==='lawyer' && post.user.lawyer) { %>
                                                <p class="text-sm text-blue-600 font-semibold"><%= post.user.lawyer.lawFirm %></p>
                                                <span class="text-gray-300">•</span>
                                            <% } %>
                                            <p class="text-sm text-gray-500 font-medium"><%= new Date(post.createdAt).toLocaleDateString() %></p>
                                            <span class="text-gray-300">•</span>
                                            <div class="flex items-center space-x-1 text-gray-500">
                                                <i class="fas fa-globe-americas text-xs"></i>
                                                <span class="text-xs font-medium">Public</span>
                                            </div>
                                        </div>
                                    </div>
                                </a>
                                <% if (locals.loggedInUserId && (post.userId === locals.loggedInUserId || post.authorId === locals.loggedInUserId)) { %>
                                    <div class="ml-2 flex space-x-3 items-center">
                                        <button class="p-1.5 text-gray-500 hover:text-blue-500 transition-all duration-200 ease-in-out edit-post-btn" title="Edit Post" data-post-id="<%= post.id %>" data-title="<%- post.title.replace(/&/g, '&amp;').replace(/\"/g, '&quot;') %>" data-content="<%- post.content.replace(/&/g, '&amp;').replace(/\"/g, '&quot;') %>">
                                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M15.232 5.232l3.536 3.536M9 13l6.586-6.586a2 2 0 112.828 2.828L11.828 15.828a2 2 0 01-2.828 0L9 13zm0 0V21h8" /></svg>
                                        </button>
                                        <button class="p-1.5 text-gray-500 hover:text-red-500 transition-all duration-200 ease-in-out delete-post-btn" title="Delete Post" data-post-id="<%= post.id %>">
                                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" /></svg>
                                        </button>
                                    </div>
                                <% } %>
                            </div>
                            <!-- Enhanced Post Content -->
                            <div class="w-full mb-6">
                                <div class="mb-4">
                                    <h5 class="font-bold text-2xl bg-gradient-to-r from-blue-600 to-indigo-600 bg-clip-text text-transparent mb-4 leading-tight"><%= post.title %></h5>
                                    <div class="prose prose-lg max-w-none">
                                        <p class="text-gray-800 text-lg leading-relaxed font-medium tracking-wide"><%= post.content.length > 200 ? post.content.substring(0, 200) + '...' : post.content %></p>
                                        <% if (post.content.length > 200) { %>
                                            <button class="text-blue-600 hover:text-blue-800 font-semibold text-sm mt-2 transition-colors">Read more</button>
                                        <% } %>
                                    </div>
                                </div>
                                <% if (post.post_photos && post.post_photos.length > 0) { %>
                                    <div class="mt-6">
                                        <% if (post.post_photos.length === 1) { %>
                                            <% post.post_photos.forEach(postPhoto => { %>
                                                <% if (postPhoto.photo && postPhoto.photo.photoPath) { %>
                                                    <div class="relative group overflow-hidden rounded-2xl shadow-2xl">
                                                        <img src="<%= postPhoto.photo.photoPath.startsWith('/') ? postPhoto.photo.photoPath : '/' + postPhoto.photo.photoPath %>" alt="Post Image" class="w-full h-auto object-cover transition-transform duration-700 group-hover:scale-110" style="max-height: 600px;">
                                                        <div class="absolute inset-0 bg-gradient-to-t from-black/30 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
                                                        <button class="absolute top-4 right-4 bg-white/90 backdrop-blur-sm rounded-full p-3 opacity-0 group-hover:opacity-100 transition-all duration-300 hover:bg-white hover:scale-110 shadow-lg">
                                                            <i class="fas fa-expand text-gray-700"></i>
                                                        </button>
                                                    </div>
                                                <% } %>
                                            <% }); %>
                                        <% } else { %>
                                            <div class="grid grid-cols-2 gap-4">
                                                <% post.post_photos.forEach(postPhoto => { %>
                                                    <% if (postPhoto.photo && postPhoto.photo.photoPath) { %>
                                                        <div class="relative group overflow-hidden rounded-xl shadow-xl">
                                                            <img src="<%= postPhoto.photo.photoPath.startsWith('/') ? postPhoto.photo.photoPath : '/' + postPhoto.photo.photoPath %>" alt="Post Image" class="w-full h-64 object-cover transition-transform duration-500 group-hover:scale-105">
                                                            <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                                                        </div>
                                                    <% } %>
                                                <% }); %>
                                            </div>
                                        <% } %>
                                    </div>
                                <% } %>
                            </div>
                            <!-- Enhanced Post Stats -->
                            <div class="flex justify-between items-center text-sm text-gray-500 border-t pt-4 mb-3">
                                <div class="flex items-center space-x-3">
                                    <% if (post.reacts && post.reacts.length > 0) { %>
                                        <div class="flex items-center bg-gray-50 rounded-full px-3 py-1 hover:bg-gray-100 transition-colors cursor-pointer" id="reactionStats-<%= post.id %>">
                                            <div class="flex -space-x-1 mr-2">
                                                <% Object.keys(groupedReactions).slice(0, 3).forEach(key => { %>
                                                    <span class="text-lg bg-white rounded-full p-1 shadow-sm border border-gray-200"><%= reactionTypes[key] %></span>
                                                <% }); %>
                                            </div>
                                            <span class="font-medium text-gray-700 reaction-count"><%= post.reacts.length %></span>
                                        </div>
                                    <% } else { %>
                                        <div class="hidden flex items-center bg-gray-50 rounded-full px-3 py-1 hover:bg-gray-100 transition-colors cursor-pointer" id="reactionStats-<%= post.id %>">
                                            <div class="flex -space-x-1 mr-2" id="reactionIcons-<%= post.id %>"></div>
                                            <span class="font-medium text-gray-700 reaction-count">0</span>
                                        </div>
                                    <% } %>
                                </div>
                                <div class="flex items-center space-x-1 hover:text-blue-600 cursor-pointer transition-colors" onclick="toggleComments(<%= post.id %>)">
                                    <i class="far fa-comment text-sm"></i>
                                    <span class="comment-count font-medium"><%= post.comments ? post.comments.length : 0 %> comments</span>
                                </div>
                            </div>
                            <!-- Enhanced Post Actions -->
                            <div class="flex justify-around border-t pt-4 relative overflow-visible">
                                <div class="relative">
                                    <button class="flex items-center space-x-2 text-gray-600 hover:text-blue-600 font-medium like-button transition-all duration-300 transform hover:scale-105 px-4 py-2 rounded-xl hover:bg-blue-50 group" onclick="toggleReactionPopup(<%= post.id %>, this)">
                                        <i class="far fa-thumbs-up text-lg group-hover:animate-bounce"></i>
                                        <span class="font-semibold">Like</span>
                                        <div class="absolute -top-1 -right-1 w-3 h-3 bg-red-500 rounded-full opacity-0 group-hover:opacity-100 transition-opacity duration-300 animate-pulse"></div>
                                    </button>
                                    <div id="reactionPopup-<%= post.id %>" class="hidden absolute bg-gradient-to-br from-white/95 to-blue-50/95 backdrop-blur-xl rounded-2xl shadow-2xl p-3 z-50 border border-white/30 reaction-popup" style="bottom: 120%; left: 50%; transform: translateX(-50%); min-width: 260px; animation: slideUp 0.3s ease-out; margin-bottom: 10px;">
                                        <div class="text-center mb-2">
                                            <h6 class="text-xs font-semibold text-gray-600 mb-1">Choose your reaction</h6>
                                            <div class="w-8 h-0.5 bg-gradient-to-r from-blue-400 to-purple-400 mx-auto rounded-full"></div>
                                        </div>
                                        <div class="flex space-x-1 justify-center">
                                            <% Object.entries(reactionTypes).forEach(([key, emoji]) => { %>
                                                <button onclick="submitReaction(<%= post.id %>, '<%= key %>', this)" class="reaction-btn text-3xl hover:scale-125 transition-all duration-300 transform hover:-translate-y-1 p-2 rounded-xl hover:bg-white/70 hover:shadow-lg relative group" title="<%= key.charAt(0).toUpperCase() + key.slice(1) %>" data-reaction="<%= key %>">
                                                    <span class="block transform group-hover:rotate-12 transition-transform duration-300"><%= emoji %></span>
                                                    <div class="absolute -bottom-10 left-1/2 transform -translate-x-1/2 bg-gray-800 text-white text-xs px-2 py-1 rounded opacity-0 group-hover:opacity-100 transition-opacity duration-300 whitespace-nowrap z-60"><%= key.charAt(0).toUpperCase() + key.slice(1) %></div>
                                                    <div class="absolute inset-0 rounded-2xl bg-gradient-to-r from-pink-400/20 to-purple-400/20 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                                                </button>
                                            <% }); %>
                                        </div>
                                        <div class="absolute -bottom-3 left-1/2 transform -translate-x-1/2 w-0 h-0 border-l-12 border-l-transparent border-r-12 border-r-transparent border-t-12 border-t-white/95 filter drop-shadow-lg"></div>
                                        <div class="absolute inset-0 rounded-3xl bg-gradient-to-br from-blue-400/10 to-purple-400/10 pointer-events-none"></div>
                                    </div>
                                </div>
                                <button class="flex items-center space-x-2 text-gray-600 hover:text-green-600 font-medium transition-all duration-300 transform hover:scale-105 px-4 py-2 rounded-xl hover:bg-green-50" onclick="toggleCommentForm(<%= post.id %>)">
                                    <i class="far fa-comment text-lg"></i><span class="font-semibold">Comment</span>
                                </button>
                                <% if (locals.loggedInUserId) { %>
                                    <button class="flex items-center space-x-2 text-gray-600 hover:text-purple-600 font-medium transition-all duration-300 transform hover:scale-105 px-4 py-2 rounded-xl hover:bg-purple-50" onclick="sharePost(<%= post.id %>)">
                                        <i class="far fa-share text-lg"></i><span class="font-semibold">Share</span>
                                    </button>
                                <% } else { %>
                                    <button class="flex items-center space-x-2 text-gray-400 font-medium px-4 py-2 rounded-xl cursor-not-allowed" disabled>
                                        <i class="far fa-share text-lg"></i><span class="font-semibold">Share</span>
                                    </button>
                                <% } %>
                            </div>
                            <div class="px-4 py-2 border-t border-gray-200 mt-3">
                                <% if (post.comments && post.comments.length > 0) { %>
                                    <button id="showCommentsBtn-<%= post.id %>" class="text-sm text-blue-600 hover:text-blue-800 mb-3" onclick="toggleComments(<%= post.id %>)">Show Comments (<%= post.comments.length %>)</button>
                                    <div id="commentsList-<%= post.id %>" class="space-y-3 hidden">
                                        <% const initialComments = post.comments.slice(0, 4); const hasMoreComments = post.comments.length > 4; %>
                                        <% initialComments.forEach(comment => { %>
                                            <div class="flex items-start space-x-3 group comment-item" data-comment-id="<%= comment.id %>">
                                                <a href="/in/<%= comment.user ? comment.user.id : '#' %>" class="hover:opacity-80 transition-opacity">
                                                    <img src="https://ui-avatars.com/api/?name=<%= comment.user ? comment.user.firstName + ' ' + comment.user.lastName : 'User' %>" alt="Commenter" class="rounded-full shadow-lg ring-2 ring-white" style="width: 36px; height: 36px; object-fit: cover;">
                                                </a>
                                                <div>
                                                    <div class="mb-1">
                                                        <a href="/in/<%= comment.user ? comment.user.id : '#' %>" class="text-sm font-semibold text-gray-900 hover:text-blue-600 transition-colors">
                                                            <%= comment.user ? comment.user.firstName + ' ' + comment.user.lastName : 'User' %>
                                                        </a>
                                                    </div>
                                                    <div class="bg-gradient-to-r from-blue-50 to-indigo-50 rounded-2xl px-4 py-3 relative shadow-sm border border-blue-100 hover:shadow-md transition-all duration-300">
                                                <div class="pr-16">
                                                    <p class="text-sm text-gray-800 break-words whitespace-normal leading-relaxed" style="word-wrap: break-word; max-width: 100%; overflow-wrap: break-word;"><%= comment.content.replace(/(.{50})/g, "$1\n") %></p>
                                                </div>
                                                <% if (locals.loggedInUserId && comment.userId === locals.loggedInUserId) { %>
                                                    <div class="absolute right-3 top-3">
                                                        <div class="flex space-x-1 items-center opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                                                            <button class="p-1.5 text-gray-400 hover:text-blue-500 transition-all duration-200 ease-in-out edit-comment-inline-btn rounded-full hover:bg-blue-100" title="Edit Comment" data-comment-id="<%= comment.id %>" data-content="<%- comment.content.replace(/&/g, '&amp;').replace(/\"/g, '&quot;') %>">
                                                                <i class="fas fa-edit text-xs"></i>
                                                            </button>
                                                            <button class="p-1.5 text-gray-400 hover:text-red-500 transition-all duration-200 ease-in-out delete-comment-btn rounded-full hover:bg-red-100" title="Delete Comment" data-comment-id="<%= comment.id %>" data-post-id="<%= post.id %>">
                                                                <i class="fas fa-trash text-xs"></i>
                                                            </button>
                                                        </div>
                                                    </div>
                                                <% } %>
                                            </div>
                                                    <div class="mt-2 flex items-center justify-between">
                                                        <div class="flex items-center space-x-4 text-xs text-gray-500">
                                                            <span class="font-medium"><%= new Date(comment.createdAt).toLocaleDateString() %></span>
                                                            <button class="hover:text-blue-600 transition-colors font-medium">Like</button>
                                                            <button class="hover:text-blue-600 transition-colors font-medium">Reply</button>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        <% }); %>
                                        <% if (hasMoreComments) { %>
                                            <div id="showMoreComments-<%= post.id %>" class="text-center">
                                                <button class="text-sm text-blue-600 hover:text-blue-800" onclick="showMoreComments(<%= post.id %>)">Show More Comments</button>
                                            </div>
                                            <div id="additionalComments-<%= post.id %>" class="hidden space-y-3">
                                                <% post.comments.slice(4).forEach(comment => { %>
                                                    <div class="flex items-start space-x-3">
                                                        <a href="/in/<%= comment.user ? comment.user.id : '#' %>" class="hover:opacity-80 transition-opacity">
                                                            <img src="https://ui-avatars.com/api/?name=<%= comment.user ? comment.user.firstName + ' ' + comment.user.lastName : 'User' %>" alt="Commenter" class="rounded-full shadow" style="width: 32px; height: 32px; object-fit: cover;">
                                                        </a>
                                                        <div>
                                                            <div class="mb-1">
                                                                <a href="/in/<%= comment.user ? comment.user.id : '#' %>" class="text-sm font-semibold text-gray-900 hover:text-blue-600 transition-colors">
                                                                    <%= comment.user ? comment.user.firstName + ' ' + comment.user.lastName : 'User' %>
                                                                </a>
                                                            </div>
                                                            <div class="bg-blue-50 rounded-lg px-3 py-2 relative">
                                                                <div class="pr-16">
                                                                    <p class="text-sm text-gray-800 break-words whitespace-normal" style="word-wrap: break-word; max-width: 100%; overflow-wrap: break-word;"><%= comment.content.replace(/(.{50})/g, "$1\n") %></p>
                                                                </div>
                                                                <% if (locals.loggedInUserId && comment.userId === locals.loggedInUserId) { %>
                                                                    <div class="absolute right-2 top-2">
                                                                        <span class="flex space-x-1 items-center">
                                                                            <button class="p-1 text-gray-500 hover:text-blue-500 transition-all duration-200 ease-in-out edit-comment-inline-btn" title="Edit Comment" data-comment-id="<%= comment.id %>" data-content="<%- comment.content.replace(/&/g, '&amp;').replace(/\"/g, '&quot;') %>">
                                                                                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M15.232 5.232l3.536 3.536M9 13l6.586-6.586a2 2 0 112.828 2.828L11.828 15.828a2 2 0 01-2.828 0L9 13zm0 0V21h8" /></svg>
                                                                            </button>
                                                                            <button class="p-1 text-gray-500 hover:text-red-500 transition-all duration-200 ease-in-out delete-comment-btn" title="Delete Comment" data-comment-id="<%= comment.id %>" data-post-id="<%= post.id %>">
                                                                                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" /></svg>
                                                                            </button>
                                                                        </span>
                                                                    </div>
                                                                <% } %>
                                                            </div>
                                                            <div class="mt-2 flex items-center justify-between">
                                                        <div class="flex items-center space-x-4 text-xs text-gray-500">
                                                            <span class="font-medium"><%= new Date(comment.createdAt).toLocaleDateString() %></span>
                                                            <button class="hover:text-blue-600 transition-colors font-medium">Like</button>
                                                            <button class="hover:text-blue-600 transition-colors font-medium">Reply</button>
                                                        </div>
                                                    </div>
                                                        </div>
                                                    </div>
                                                <% }); %>
                                            </div>
                                        <% } %>
                                    </div>
                                <% } %>
                                <% if (locals.loggedInUserId) { %>
                                    <div id="commentForm-<%= post.id %>" class="mt-4 flex items-start space-x-3 border-t pt-4 hidden">
                                        <img src="https://ui-avatars.com/api/?background=random" alt="Your avatar" class="rounded-full shadow-lg ring-2 ring-white" style="width: 40px; height: 40px; object-fit: cover;">
                                        <form class="flex-1" onsubmit="event.preventDefault(); submitComment(this, <%= post.id %>);">
                                            <div class="relative">
                                                <input type="text" name="content" placeholder="Write a comment..." class="w-full border border-gray-200 rounded-2xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 bg-gray-50 hover:bg-white transition-all duration-300 pr-12">
                                                <button type="submit" class="absolute right-2 top-1/2 transform -translate-y-1/2 bg-blue-600 text-white rounded-full p-2 hover:bg-blue-700 transition-all duration-300 transform hover:scale-105 shadow-lg">
                                                    <i class="fas fa-paper-plane text-xs"></i>
                                                </button>
                                            </div>
                                        </form>
                                    </div>
                                <% } %>
                            </div>
                        </div>
                    </div>
                    <% }); %>
                <% } else { %>
                    <div class="bg-white rounded-2xl shadow-lg mb-8">
                        <div class="p-8">
                            <div class="bg-blue-100 text-blue-700 rounded-xl p-6 text-center text-lg font-medium">
                                No posts found. <% if (locals.loggedInUserId && locals.userRole === 'lawyer') { %><a href="#" onclick="togglePostModal()" class="text-blue-600 underline">Share your legal insights</a><% } %>
                            </div>
                        </div>
                    </div>
                <% } %>
                <!-- Modal for Creating a Post (Lawyers Only) -->
                <% if (locals.userRole === 'lawyer') { %>
                <div id="postModal" class="fixed inset-0 bg-black bg-opacity-50 hidden flex items-center justify-center z-50">
                    <div class="bg-white rounded-2xl shadow-2xl w-96 p-8 relative">
                        <button onclick="togglePostModal()" class="absolute top-2 right-2 text-gray-500 hover:text-gray-700 text-2xl">&times;</button>
                        <h3 class="text-2xl font-bold mb-6 text-blue-700">Share Legal Insights</h3>
                        <form action="/in/<%= typeof user !== 'undefined' && user ? user.id : '' %>/create-post" method="POST" enctype="multipart/form-data">
                            <div class="mb-5">
                                <label for="title" class="block text-md font-medium text-gray-700 mb-1">Article Title</label>
                                <input type="text" name="title" id="title" required class="w-full border border-blue-200 rounded-lg px-4 py-2 focus:outline-none focus:ring-1 focus:ring-blue-500" placeholder="e.g., Understanding Employment Rights">
                            </div>
                            <div class="mb-5">
                                <label for="content" class="block text-md font-medium text-gray-700 mb-1">Legal Content</label>
                                <textarea name="content" id="content" rows="4" required class="w-full border border-blue-200 rounded-lg px-4 py-2 focus:outline-none focus:ring-1 focus:ring-blue-500" placeholder="Share your legal expertise and insights..."></textarea>
                            </div>
                            <div class="mb-5">
                                <label for="image" class="block text-md font-medium text-gray-700 mb-1">Supporting Image (optional)</label>
                                <input type="file" name="image" id="image" accept="image/*" class="w-full border border-blue-200 rounded-lg px-4 py-2 focus:outline-none focus:ring-1 focus:ring-blue-500">
                            </div>
                            <button type="submit" class="w-full bg-blue-600 text-white py-3 rounded-lg hover:bg-blue-700 focus:outline-none font-semibold">Share Insights</button>
                        </form>
                    </div>
                </div>
                <% } %>
                <!-- Right Sidebar -->
                <div class="lg:col-span-3 space-y-8">
                    <div class="bg-white rounded-2xl shadow-lg">
                        <div class="border-b px-6 py-4">
                            <h6 class="font-semibold text-blue-700 text-lg">Quick Legal Help</h6>
                        </div>
                        <div class="p-8">
                            <div class="space-y-4">
                                <div class="bg-blue-50 p-4 rounded-lg border border-blue-200">
                                    <h4 class="font-semibold text-blue-800 mb-2">Need Legal Advice?</h4>
                                    <p class="text-blue-700 text-sm mb-3">Connect with qualified lawyers in your area</p>
                                    <a href="/search?type=lawyer" class="bg-blue-600 text-white px-4 py-2 rounded-lg text-sm hover:bg-blue-700 transition inline-flex items-center">
                                        <i class="fas fa-search mr-2"></i>
                                        Find Lawyers
                                    </a>
                                </div>
                                <div class="bg-green-50 p-4 rounded-lg border border-green-200">
                                    <h4 class="font-semibold text-green-800 mb-2">Job Opportunities</h4>
                                    <p class="text-green-700 text-sm mb-3">Post or find legal job opportunities</p>
                                    <a href="/jobs" class="bg-green-600 text-white px-4 py-2 rounded-lg text-sm hover:bg-green-700 transition inline-flex items-center">
                                        <i class="fas fa-briefcase mr-2"></i>
                                        Browse Jobs
                                    </a>
                                </div>
                                <div class="bg-purple-50 p-4 rounded-lg border border-purple-200">
                                    <h4 class="font-semibold text-purple-800 mb-2">Legal Resources</h4>
                                    <p class="text-purple-700 text-sm mb-3">Free guides to understand your rights</p>
                                    <a href="/resources/consumer" class="bg-purple-600 text-white px-4 py-2 rounded-lg text-sm hover:bg-purple-700 transition inline-flex items-center">
                                        <i class="fas fa-book mr-2"></i>
                                        Learn More
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="bg-white rounded-2xl shadow-lg">
                        <div class="border-b px-6 py-4">
                            <h6 class="font-semibold text-blue-700 text-lg">Connect with Lawyers</h6>
                        </div>
                        <div class="p-8">
                            <ul class="space-y-4 text-md" id="suggestedLawyers">
                                <li class="text-center text-gray-500 py-2">Loading suggestions...</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Inline JavaScript for Reaction Popup -->
    <script>
        function toggleReactionPopup(postId, buttonElement) {
            const popup = document.getElementById(`reactionPopup-${postId}`);
            
            // Close all other popups first
            document.querySelectorAll('.reaction-popup').forEach(p => {
                if (p.id !== `reactionPopup-${postId}`) {
                    p.classList.add('hidden');
                }
            });
            
            if (popup) {
                if (popup.classList.contains('hidden')) {
                    // Show with animation
                    popup.classList.remove('hidden');
                    popup.style.transform = 'translateX(-50%) scale(0.8)';
                    popup.style.opacity = '0';
                    
                    setTimeout(() => {
                        popup.style.transform = 'translateX(-50%) scale(1)';
                        popup.style.opacity = '1';
                        popup.style.transition = 'all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1)';
                    }, 10);
                    
                    // Add entrance animation to reaction buttons
                    const reactionBtns = popup.querySelectorAll('.reaction-btn');
                    reactionBtns.forEach((btn, index) => {
                        btn.style.transform = 'scale(0) rotate(180deg)';
                        setTimeout(() => {
                            btn.style.transform = 'scale(1) rotate(0deg)';
                            btn.style.transition = 'all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1)';
                        }, index * 50);
                    });
                } else {
                    // Hide with animation
                    popup.style.transform = 'translateX(-50%) scale(0.8)';
                    popup.style.opacity = '0';
                    setTimeout(() => {
                        popup.classList.add('hidden');
                        popup.style.transform = 'translateX(-50%) scale(1)';
                        popup.style.opacity = '1';
                    }, 200);
                }
            }
            
            // Close popup when clicking outside
            setTimeout(() => {
                document.addEventListener('click', function closePopup(e) {
                    if (!popup.contains(e.target) && !buttonElement.contains(e.target)) {
                        popup.classList.add('hidden');
                        document.removeEventListener('click', closePopup);
                    }
                });
            }, 100);
        }
        function submitReaction(postId, reaction, reactionButtonElement) {
            const likeButton = reactionButtonElement.closest('.flex.justify-around').querySelector('.like-button');
            const popup = document.getElementById(`reactionPopup-${postId}`);
            
            // Immediate visual feedback
            reactionButtonElement.style.transform = 'scale(1.3) rotate(15deg)';
            reactionButtonElement.style.filter = 'brightness(1.2)';
            
            // Create floating reaction animation
            const floatingReaction = document.createElement('div');
            floatingReaction.innerHTML = reactionButtonElement.querySelector('span').textContent;
            floatingReaction.className = 'fixed text-4xl pointer-events-none z-50 animate-ping';
            floatingReaction.style.left = reactionButtonElement.getBoundingClientRect().left + 'px';
            floatingReaction.style.top = reactionButtonElement.getBoundingClientRect().top + 'px';
            document.body.appendChild(floatingReaction);
            
            // Animate floating reaction
            setTimeout(() => {
                floatingReaction.style.transform = 'translateY(-100px) scale(2)';
                floatingReaction.style.opacity = '0';
                floatingReaction.style.transition = 'all 1s ease-out';
            }, 100);
            
            setTimeout(() => {
                if (document.body.contains(floatingReaction)) {
                    document.body.removeChild(floatingReaction);
                }
            }, 1100);
            
            fetch(`/post/${postId}/react`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({ reaction: reaction })
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    // Update the like button with enhanced animation
                    if (likeButton) {
                        const icon = likeButton.querySelector('i');
                        const span = likeButton.querySelector('span');
                        if (icon && span) {
                            // Add reaction emoji to button
                            const reactionEmoji = reactionTypes[reaction] || '👍';
                            span.innerHTML = `${reactionEmoji} ${reaction.charAt(0).toUpperCase() + reaction.slice(1)}`;
                            icon.className = 'fas fa-thumbs-up text-lg group-hover:animate-bounce';
                            
                            // Add active state styling
                            likeButton.classList.add('text-blue-600', 'bg-blue-50');
                            likeButton.classList.remove('text-gray-600');
                        }
                    }
                    
                    // Update reaction display with animation
                    updateReactionDisplay(postId, data.reactions, data.userReaction);
                    
                    // Hide popup with animation
                    if (popup) {
                        popup.style.transform = 'translateX(-50%) scale(0.8)';
                        popup.style.opacity = '0';
                        setTimeout(() => {
                            popup.classList.add('hidden');
                            popup.style.transform = 'translateX(-50%) scale(1)';
                            popup.style.opacity = '1';
                        }, 200);
                    }
                    
                    // Enhanced visual feedback
                    setTimeout(() => {
                        reactionButtonElement.style.transform = 'scale(1)';
                        reactionButtonElement.style.filter = 'brightness(1)';
                    }, 300);
                    
                    // Show success notification with reaction emoji
                    showNotification(`${reactionTypes[reaction]} Reaction added!`, 'success');
                    
                    // Add ripple effect to like button
                    createRippleEffect(likeButton);
                    
                } else {
                    // Reset visual state on error
                    setTimeout(() => {
                        reactionButtonElement.style.transform = 'scale(1)';
                        reactionButtonElement.style.filter = 'brightness(1)';
                    }, 300);
                    
                    console.error('Failed to submit reaction:', data.message);
                    showNotification('Failed to react: ' + data.message, 'error');
                }
            })
            .catch(error => {
                // Reset visual state on error
                setTimeout(() => {
                    reactionButtonElement.style.transform = 'scale(1)';
                    reactionButtonElement.style.filter = 'brightness(1)';
                }, 300);
                
                console.error('Error submitting reaction:', error);
                showNotification('Error reacting to post.', 'error');
            });
        }

        window.updateReactionDisplay = function(postId, reactions, userReaction) {
            const reactionStats = document.getElementById(`reactionStats-${postId}`);
            const reactionIcons = document.getElementById(`reactionIcons-${postId}`);
            const reactionCount = reactionStats ? reactionStats.querySelector('.reaction-count') : null;
            const likeButton = document.querySelector(`#post-${postId} .like-button`);
            
            // Update like button based on user's reaction
            if (likeButton) {
                const icon = likeButton.querySelector('i');
                const span = likeButton.querySelector('span');
                if (icon && span) {
                    if (userReaction) {
                        // User has reacted - show their reaction
                        const reactionEmoji = getReactionEmoji(userReaction);
                        span.innerHTML = `${reactionEmoji} ${userReaction.charAt(0).toUpperCase() + userReaction.slice(1)}`;
                        icon.className = 'fas fa-thumbs-up text-lg group-hover:animate-bounce';
                        likeButton.classList.add('text-blue-600', 'bg-blue-50');
                        likeButton.classList.remove('text-gray-600');
                    } else {
                        // User has not reacted - show default state
                        span.textContent = 'Like';
                        icon.className = 'far fa-thumbs-up text-lg group-hover:animate-bounce';
                        likeButton.classList.remove('text-blue-600', 'bg-blue-50');
                        likeButton.classList.add('text-gray-600');
                    }
                }
            }
            
            if (reactions && reactions.length > 0) {
                if (reactionStats) {
                    reactionStats.classList.remove('hidden');
                    // Add bounce animation to stats
                    reactionStats.style.transform = 'scale(1.1)';
                    setTimeout(() => {
                        reactionStats.style.transform = 'scale(1)';
                        reactionStats.style.transition = 'transform 0.3s ease-out';
                    }, 200);
                }
                
                if (reactionCount) {
                    // Animate count change
                    reactionCount.style.transform = 'scale(1.3)';
                    reactionCount.style.color = '#3B82F6';
                    reactionCount.textContent = reactions.length;
                    setTimeout(() => {
                        reactionCount.style.transform = 'scale(1)';
                        reactionCount.style.color = '';
                        reactionCount.style.transition = 'all 0.3s ease-out';
                    }, 300);
                }
                
                // Update reaction icons with staggered animation
                if (reactionIcons) {
                    const oldIcons = reactionIcons.querySelectorAll('span');
                    oldIcons.forEach((icon, index) => {
                        setTimeout(() => {
                            icon.style.transform = 'scale(0) rotate(180deg)';
                            icon.style.opacity = '0';
                        }, index * 50);
                    });
                    
                    setTimeout(() => {
                        reactionIcons.innerHTML = '';
                        const uniqueReactions = [...new Set(reactions.map(r => r.reaction || r.reactionType))];
                        const reactionCounts = {};
                        reactions.forEach(r => {
                            const type = r.reaction || r.reactionType;
                            reactionCounts[type] = (reactionCounts[type] || 0) + 1;
                        });
                        
                        uniqueReactions.slice(0, 3).forEach((type, index) => {
                            const emoji = getReactionEmoji(type);
                            const count = reactionCounts[type];
                            const iconElement = document.createElement('span');
                            iconElement.className = 'inline-flex items-center text-lg bg-white rounded-full p-2 shadow-lg border border-gray-200 hover:shadow-xl transition-all duration-300 transform hover:scale-110 relative group';
                            iconElement.innerHTML = `
                                ${emoji}
                                <span class="absolute -top-2 -right-2 bg-blue-500 text-white text-xs rounded-full w-5 h-5 flex items-center justify-center font-bold">${count}</span>
                                <div class="absolute -bottom-8 left-1/2 transform -translate-x-1/2 bg-gray-800 text-white text-xs px-2 py-1 rounded opacity-0 group-hover:opacity-100 transition-opacity duration-300 whitespace-nowrap">${type.charAt(0).toUpperCase() + type.slice(1)}: ${count}</div>
                            `;
                            iconElement.style.transform = 'scale(0) rotate(-180deg)';
                            iconElement.style.opacity = '0';
                            reactionIcons.appendChild(iconElement);
                            
                            setTimeout(() => {
                                iconElement.style.transform = 'scale(1) rotate(0deg)';
                                iconElement.style.opacity = '1';
                                iconElement.style.transition = 'all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1)';
                            }, index * 100 + 200);
                        });
                    }, 200);
                }
            } else {
                if (reactionStats) {
                    reactionStats.style.transform = 'scale(0.8)';
                    reactionStats.style.opacity = '0';
                    setTimeout(() => {
                        reactionStats.classList.add('hidden');
                        reactionStats.style.transform = 'scale(1)';
                        reactionStats.style.opacity = '1';
                    }, 300);
                }
                
                // Reset like button to default state when no reactions
                if (likeButton && !userReaction) {
                    const span = likeButton.querySelector('span');
                    if (span) {
                        span.innerHTML = `<i class="far fa-thumbs-up text-lg group-hover:animate-bounce"></i> Like`;
                        likeButton.classList.remove('text-blue-600', 'bg-blue-50');
                        likeButton.classList.add('text-gray-600');
                    }
                }
            }
        }

        function getReactionEmoji(type) {
            const emojis = {
                'like': '👍',
                'love': '❤️',
                'haha': '😂',
                'laugh': '😂',
                'wow': '😮',
                'sad': '😢',
                'angry': '😠'
            };
            return emojis[type] || '👍';
        }

        function createRippleEffect(element) {
            const ripple = document.createElement('div');
            const rect = element.getBoundingClientRect();
            const size = Math.max(rect.width, rect.height);
            
            ripple.style.width = ripple.style.height = size + 'px';
            ripple.style.left = (rect.width / 2 - size / 2) + 'px';
            ripple.style.top = (rect.height / 2 - size / 2) + 'px';
            ripple.className = 'absolute rounded-full bg-blue-400 opacity-30 animate-ping pointer-events-none';
            
            element.style.position = 'relative';
            element.style.overflow = 'hidden';
            element.appendChild(ripple);
            
            setTimeout(() => {
                if (element.contains(ripple)) {
                    element.removeChild(ripple);
                }
            }, 600);
        }
        
        function showNotification(message, type = 'info') {
            const notification = document.createElement('div');
            notification.className = `fixed top-4 right-4 z-50 px-6 py-4 rounded-2xl shadow-2xl transform transition-all duration-500 translate-x-full backdrop-blur-lg border border-white/20`;
            
            const colors = {
                success: 'bg-gradient-to-r from-green-500 to-emerald-500 text-white',
                error: 'bg-gradient-to-r from-red-500 to-pink-500 text-white',
                warning: 'bg-gradient-to-r from-yellow-500 to-orange-500 text-white',
                info: 'bg-gradient-to-r from-blue-500 to-indigo-500 text-white'
            };
            
            const icons = {
                success: '✅',
                error: '❌',
                warning: '⚠️',
                info: 'ℹ️'
            };
            
            notification.className += ` ${colors[type] || colors.info}`;
            notification.innerHTML = `
                <div class="flex items-center space-x-3">
                    <span class="text-xl">${icons[type] || icons.info}</span>
                    <span class="font-semibold">${message}</span>
                </div>
            `;
            
            document.body.appendChild(notification);
            
            setTimeout(() => {
                notification.style.transform = 'translateX(0) scale(1.05)';
            }, 100);
            
            setTimeout(() => {
                notification.style.transform = 'translateX(0) scale(1)';
            }, 200);
            
            setTimeout(() => {
                notification.style.transform = 'translateX(100%) scale(0.8)';
                setTimeout(() => {
                    if (document.body.contains(notification)) {
                        document.body.removeChild(notification);
                    }
                }, 500);
            }, 3000);
        }

        function togglePostModal() {
        const modal = document.getElementById('postModal');
        modal.classList.toggle('hidden');
        document.body.classList.toggle('overflow-hidden'); // Prevent scrolling when modal is open
        
        // Reset form when opening modal
        if (!modal.classList.contains('hidden')) {
            const form = modal.querySelector('form');
            if (form) form.reset();
        }
    }
    </script>
    <script src="/js/feed.js"></script>

    <!-- Font Awesome CDN for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <script>
    // Fetch suggested lawyers when the page loads
    document.addEventListener('DOMContentLoaded', function() {
        fetchSuggestedLawyers();
        
        // Function to fetch suggested lawyers
        function fetchSuggestedLawyers() {
            fetch('/mynetwork/suggested-lawyers')
                .then(response => {
                    if (!response.ok) {
                        throw new Error('Network response was not ok');
                    }
                    return response.json();
                })
                .then(data => {
                    displaySuggestedLawyers(data.lawyers);
                })
                .catch(error => {
                    console.error('Error fetching suggested lawyers:', error);
                    document.getElementById('suggestedLawyers').innerHTML = 
                        '<li class="text-center text-gray-500 py-2">Failed to load suggestions</li>';
                });
        }
        
        // Function to display suggested lawyers
        function displaySuggestedLawyers(lawyers) {
            const container = document.getElementById('suggestedLawyers');
            
            if (!lawyers || lawyers.length === 0) {
                container.innerHTML = '<li class="text-center text-gray-500 py-2">No suggestions available</li>';
                return;
            }
            
            let html = '';
            lawyers.forEach(lawyer => {
                const imageSrc = lawyer.ProfileImage && lawyer.ProfileImage.imagePath ?
                    lawyer.ProfileImage.imagePath :
                    `https://ui-avatars.com/api/?name=${lawyer.firstName}+${lawyer.lastName}`;
                    
                html += `
                <li class="flex items-center">
                    <img src="${imageSrc}" alt="${lawyer.firstName}'s Profile" class="rounded-full mr-3"
                        style="width: 40px; height: 40px; object-fit: cover;">
                    <div class="flex-1">
                        <h6 class="font-semibold">${lawyer.firstName} ${lawyer.lastName}</h6>
                        <p class="text-gray-500">${lawyer.Lawyer ? lawyer.Lawyer.lawFirm : 'Lawyer'}</p>
                    </div>
                    <button class="connect-btn text-blue-600 border border-blue-600 rounded px-2 py-1 text-xs"
                        data-user-id="${lawyer.id}">Connect</button>
                </li>`;
            });
            
            container.innerHTML = html;
            
            // Add event listeners to connect buttons
            document.querySelectorAll('.connect-btn').forEach(button => {
                button.addEventListener('click', function() {
                    const userId = this.getAttribute('data-user-id');
                    sendConnectionRequest(userId, this);
                });
            });
        }
        
        // Function to send connection request
        function sendConnectionRequest(userId, button) {
            button.disabled = true;
            button.textContent = 'Sending...';
            
            fetch(`/mynetwork/connect/${userId}`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                }
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    button.textContent = 'Request Sent';
                    button.classList.remove('text-blue-600', 'border-blue-600');
                    button.classList.add('text-gray-400', 'border-gray-400');
                } else {
                    button.textContent = 'Connect';
                    button.disabled = false;
                    alert(data.message);
                }
            })
            .catch(error => {
                console.error('Error:', error);
                button.textContent = 'Connect';
                button.disabled = false;
                alert('An error occurred. Please try again.');
            });
        }
        
        // Function to share a post
        function sharePost(postId) {
            fetch(`/feed/posts/${postId}/share`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                }
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    showNotification(data.message, 'success');
                } else {
                    showNotification(data.error || 'Failed to share post', 'error');
                }
            })
            .catch(error => {
                console.error('Error sharing post:', error);
                showNotification('An error occurred while sharing the post', 'error');
            });
        }
    });
    </script>
<!-- Delete Confirmation Modal -->
<div id="deleteConfirmModal" class="fixed inset-0 z-60 flex items-center justify-center bg-black bg-opacity-40 hidden">
  <div class="bg-white rounded-xl shadow-lg p-8 max-w-sm w-full text-center">
    <h3 class="text-lg font-semibold mb-4 text-gray-800">Are you sure you want to delete?</h3>
    <div class="flex justify-center space-x-4 mt-6">
      <button onclick="confirmDelete()" class="px-6 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700 transition">Delete</button>
      <button onclick="hideDeleteModal()" class="px-6 py-2 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 transition">Cancel</button>
    </div>
  </div>
</div>

<!-- Edit Post Modal -->
<div id="editPostModal" class="fixed inset-0 bg-black bg-opacity-50 hidden flex items-center justify-center z-50">
    <div class="bg-white rounded-2xl shadow-2xl w-96 p-8 relative">
        <button onclick="toggleEditPostModal()" class="absolute top-2 right-2 text-gray-500 hover:text-gray-700 text-2xl">&times;</button>
        <h3 class="text-2xl font-bold mb-6 text-blue-700">Edit Post</h3>
        <form id="editPostForm">
            <input type="hidden" name="postId" id="editPostId">
            <div class="mb-5">
                <label for="editTitle" class="block text-md font-medium text-gray-700 mb-1">Title</label>
                <input type="text" name="title" id="editTitle" required class="w-full border border-blue-200 rounded-lg px-4 py-2 focus:outline-none focus:ring-1 focus:ring-blue-500">
            </div>
            <div class="mb-5">
                <label for="editContent" class="block text-md font-medium text-gray-700 mb-1">Content</label>
                <textarea name="content" id="editContent" rows="4" required class="w-full border border-blue-200 rounded-lg px-4 py-2 focus:outline-none focus:ring-1 focus:ring-blue-500"></textarea>
            </div>
            <button type="submit" class="w-full bg-blue-600 text-white py-3 rounded-lg hover:bg-blue-700 focus:outline-none font-semibold">Save Changes</button>
        </form>
    </div>
</div>
<!-- Edit Comment Modal -->
<div id="editCommentModal" class="fixed inset-0 bg-black bg-opacity-50 hidden flex items-center justify-center z-50">
    <div class="bg-white rounded-2xl shadow-2xl w-96 p-8 relative">
        <button onclick="toggleEditCommentModal()" class="absolute top-2 right-2 text-gray-500 hover:text-gray-700 text-2xl">&times;</button>
        <h3 class="text-2xl font-bold mb-6 text-blue-700">Edit Comment</h3>
        <form id="editCommentForm">
            <input type="hidden" name="commentId" id="editCommentId">
            <div class="mb-5">
                <label for="editCommentContent" class="block text-md font-medium text-gray-700 mb-1">Content</label>
                <textarea name="content" id="editCommentContent" rows="3" required class="w-full border border-blue-200 rounded-lg px-4 py-2 focus:outline-none focus:ring-1 focus:ring-blue-500"></textarea>
            </div>
            <button type="submit" class="w-full bg-blue-600 text-white py-3 rounded-lg hover:bg-blue-700 focus:outline-none font-semibold">Save Changes</button>
        </form>
    </div>

<script>
    window.loggedInUserId = <%= locals.loggedInUserId ? locals.loggedInUserId : 'null' %>;
</script>

</div>
