<div class="max-w-6xl mx-auto px-6 py-12">
  <!-- Hero Profile Section -->
  <div class="relative bg-gradient-to-br from-slate-900 via-blue-900 to-indigo-900 rounded-3xl overflow-hidden mb-12 shadow-2xl">
    <!-- Premium Background Pattern -->
    <div class="absolute inset-0 opacity-10">
      <svg class="w-full h-full" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
        <defs>
          <pattern id="grid" width="10" height="10" patternUnits="userSpaceOnUse">
            <path d="M 10 0 L 0 0 0 10" fill="none" stroke="white" stroke-width="0.5"/>
          </pattern>
        </defs>
        <rect width="100" height="100" fill="url(#grid)" />
      </svg>
    </div>
    
    <!-- Elegant Background Overlay -->
    <div class="h-80 bg-cover bg-center relative"
      style="background-image: url('<%= profile && profile.backgroundImage ? profile.backgroundImage : 'https://images.unsplash.com/photo-1589829545856-d10d557cf95f?auto=format&fit=crop&w=1600&q=80' %>');">      
      <div class="absolute inset-0 bg-gradient-to-br from-slate-900/80 via-blue-900/70 to-indigo-900/80"></div>
      
      <!-- Premium Verification Badge -->
      <% if (profile?.role === 'lawyer' && profile?.lawyer?.verificationStatus) { %>
        <div class="absolute top-8 left-8 z-20">
          <div class="backdrop-blur-xl bg-white/10 border border-white/20 rounded-2xl px-6 py-4 shadow-2xl">
            <div class="flex items-center space-x-4">
              <div class="relative">
                <% if (profile?.lawyer?.verificationStatus === 'approved') { %>
                  <div class="w-16 h-16 bg-gradient-to-br from-emerald-400 to-emerald-600 rounded-2xl flex items-center justify-center shadow-xl">
                    <i class="fas fa-shield-check text-white text-2xl"></i>
                  </div>
                  <div class="absolute -top-2 -right-2 w-6 h-6 bg-emerald-400 rounded-full flex items-center justify-center">
                    <i class="fas fa-check text-white text-xs"></i>
                  </div>
                <% } else if (profile?.lawyer?.verificationStatus === 'pending') { %>
                  <div class="w-16 h-16 bg-gradient-to-br from-amber-400 to-amber-600 rounded-2xl flex items-center justify-center shadow-xl">
                    <i class="fas fa-hourglass-half text-white text-2xl"></i>
                  </div>
                  <div class="absolute -top-2 -right-2 w-6 h-6 bg-amber-400 rounded-full animate-pulse"></div>
                <% } else if (profile?.lawyer?.verificationStatus === 'rejected') { %>
                  <div class="w-16 h-16 bg-gradient-to-br from-red-400 to-red-600 rounded-2xl flex items-center justify-center shadow-xl">
                    <i class="fas fa-exclamation-triangle text-white text-2xl"></i>
                  </div>
                <% } else { %>
                  <div class="w-16 h-16 bg-gradient-to-br from-gray-400 to-gray-600 rounded-2xl flex items-center justify-center shadow-xl">
                    <i class="fas fa-question-circle text-white text-2xl"></i>
                  </div>
                <% } %>
              </div>
              <div class="text-white">
                <div class="text-lg font-bold tracking-wide">LEGAL AUTHORITY</div>
                <div class="flex items-center space-x-2 mt-1">
                  <span class="text-sm font-semibold
                    <% if (profile?.lawyer?.verificationStatus === 'approved') { %>
                      text-emerald-300
                    <% } else if (profile?.lawyer?.verificationStatus === 'pending') { %>
                      text-amber-300
                    <% } else if (profile?.lawyer?.verificationStatus === 'rejected') { %>
                      text-red-300
                    <% } else { %>
                      text-gray-300
                    <% } %>">
                    <% if (profile?.lawyer?.verificationStatus === 'approved') { %>
                      ✓ VERIFIED ATTORNEY
                    <% } else if (profile?.lawyer?.verificationStatus === 'pending') { %>
                      ⏳ VERIFICATION IN PROGRESS
                    <% } else if (profile?.lawyer?.verificationStatus === 'rejected') { %>
                      ✗ VERIFICATION DECLINED
                    <% } else { %>
                      ❓ STATUS UNKNOWN
                    <% } %>
                  </span>
                </div>
              </div>
            </div>
          </div>
        </div>
      <% } %>
    </div>
    
    <!-- Premium Profile Image -->
    <div class="absolute top-60 left-12 z-10">
      <div class="relative">
        <!-- Elegant Frame -->
        <div class="w-48 h-48 rounded-3xl bg-gradient-to-br from-white/20 to-white/5 backdrop-blur-xl border border-white/30 p-2 shadow-2xl">
          <div class="w-full h-full rounded-2xl overflow-hidden bg-gradient-to-br from-slate-100 to-slate-200">
            <img id="profileImage"
              src="<%= (profile.ProfileImage && profile.ProfileImage.imagePath) ? profile.ProfileImage.imagePath : 'https://ui-avatars.com/api/?name=' + encodeURIComponent(profile.firstName + ' ' + profile.lastName) + '&size=192&background=1e293b&color=ffffff&bold=true' %>"
              alt="Profile" class="w-full h-full object-cover">
          </div>
        </div>
        
        <!-- Professional Status Ring -->
        <div class="absolute -bottom-3 -right-3 w-12 h-12 bg-gradient-to-br from-emerald-400 to-emerald-600 rounded-full border-4 border-white shadow-xl flex items-center justify-center">
          <div class="w-4 h-4 bg-emerald-300 rounded-full animate-pulse"></div>
        </div>
        
        <!-- Premium Edit Overlay -->
        <% if (locals.loggedInUserId && locals.loggedInUserId === profile?.id) { %>
          <div class="absolute inset-0 bg-black/60 backdrop-blur-sm rounded-3xl flex items-center justify-center opacity-0 hover:opacity-100 transition-all duration-300 cursor-pointer group" onclick="document.getElementById('profileImageInput').click()">
            <div class="text-center transform group-hover:scale-110 transition-transform">
              <i class="fas fa-camera text-white text-3xl mb-2"></i>
              <div class="text-white text-sm font-semibold">Update Photo</div>
            </div>
          </div>
          <input type="file" id="profileImageInput" accept="image/*" style="display: none;" onchange="uploadProfileImage(this)">
        <% } %>
      </div>
    </div>
    <!-- Premium Action Buttons -->
    <div class="absolute top-8 right-8 flex space-x-4 z-20">
      <% if (locals.loggedInUserId && locals.loggedInUserId===profile?.id) { %>
        <a href="/in/edit"
          class="group flex items-center px-6 py-3 backdrop-blur-xl bg-white/10 border border-white/20 text-white rounded-2xl hover:bg-white/20 shadow-2xl transition-all duration-300 hover:scale-105">
          <i class="fas fa-pencil-alt mr-3 group-hover:rotate-12 transition-transform"></i> 
          <span class="font-semibold">Edit Profile</span>
        </a>
      <% } else { %>
        <div class="flex space-x-4">
          <button class="group flex items-center px-6 py-3 backdrop-blur-xl bg-white/10 border border-white/20 text-white rounded-2xl hover:bg-white/20 shadow-2xl transition-all duration-300 hover:scale-105">
            <i class="fas fa-comment mr-3 group-hover:scale-110 transition-transform"></i> 
            <span class="font-semibold">Message</span>
          </button>
          <form action="/mynetwork/connect/<%= profile.id %>" method="POST" class="inline">
            <button type="submit"
              class="group flex items-center px-6 py-3 bg-gradient-to-r from-blue-500 to-indigo-600 text-white rounded-2xl shadow-2xl hover:from-blue-600 hover:to-indigo-700 transition-all duration-300 hover:scale-105">
              <i class="fas fa-user-plus mr-3 group-hover:scale-110 transition-transform"></i> 
              <span class="font-semibold">Connect</span>
            </button>
          </form>
        </div>
      <% } %>
    </div>
    <!-- Premium Profile Information -->
    <div class="pt-40 pb-12 px-12">
      <div class="ml-60">
        <div class="flex items-center space-x-6 mb-4">
          <h1 class="text-6xl font-bold text-black tracking-tight drop-shadow-xl">
            <%= profile?.firstName %> <%= profile?.lastName %>
          </h1>
          <% if (profile?.role === 'lawyer' && profile?.lawyer?.verificationStatus === 'approved') { %>
            <div class="flex items-center backdrop-blur-xl bg-emerald-500/40 border border-emerald-400/60 px-4 py-2 rounded-2xl">
              <i class="fas fa-certificate text-emerald-100 text-lg mr-2"></i>
              <span class="text-black font-bold tracking-wide drop-shadow-md">VERIFIED ATTORNEY</span>
            </div>
          <% } %>
        </div>
        
        <div class="flex items-center space-x-4 mb-6">
          <div class="backdrop-blur-xl bg-white/25 border border-white/50 px-6 py-3 rounded-2xl">
            <p class="text-black font-bold text-xl tracking-wide drop-shadow-md">
              <%= profile?.role?.charAt(0).toUpperCase() + profile?.role?.slice(1) %>
            </p>
          </div>
          <% if (profile?.lawyer?.specialization) { %>
            <div class="backdrop-blur-xl bg-blue-500/40 border border-blue-400/60 px-6 py-3 rounded-2xl">
              <p class="text-black font-bold text-lg drop-shadow-md">
                <%= profile.lawyer.specialization %>
              </p>
            </div>
          <% } %>
        </div>
        
        <% if (profile?.city || profile?.country) { %>
          <div class="flex items-center text-black font-bold text-xl mb-8 drop-shadow-lg">
            <i class="fas fa-map-marker-alt mr-3 text-blue-200 drop-shadow-md"></i>
            <% if (profile?.city && profile?.country) { %>
              <%= profile.city %>, <%= profile.country %>
            <% } else if (profile?.city) { %>
              <%= profile.city %>
            <% } else if (profile?.country) { %>
              <%= profile.country %>
            <% } %>
          </div>
        <% } %>
        
        <!-- Premium Stats Display -->
        <div class="flex items-center space-x-12 mt-8">
          <div class="flex items-center text-white">
            <div class="w-16 h-16 backdrop-blur-xl bg-white/25 border border-white/50 rounded-2xl flex items-center justify-center mr-4">
              <i class="fas fa-user-friends text-blue-200 text-2xl drop-shadow-md"></i>
            </div>
            <div>
              <div class="text-4xl font-bold drop-shadow-lg"><%= connectionsCount %></div>
              <div class="text-black font-bold drop-shadow-md">Professional Network</div>
            </div>
          </div>
          
          <% if (profile?.role === 'lawyer') { %>
            <div class="flex items-center text-white">
              <div class="w-16 h-16 backdrop-blur-xl bg-white/25 border border-white/50 rounded-2xl flex items-center justify-center mr-4">
                <i class="fas fa-gavel text-purple-200 text-2xl drop-shadow-md"></i>
              </div>
              <div>
                <div class="text-4xl font-bold drop-shadow-lg"><%= new Date().getFullYear() - new Date(profile?.createdAt || new Date()).getFullYear() %></div>
                <div class="text-black font-bold drop-shadow-md">Years of Practice</div>
              </div>
            </div>
          <% } %>
        </div>
      </div>
    </div>
  </div>

  <!-- Premium About Section -->
  <div class="backdrop-blur-xl bg-white/15 border border-white/30 rounded-3xl shadow-2xl mb-12 p-10">
    <div class="flex items-center mb-8">
      <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-2xl flex items-center justify-center mr-6 shadow-xl">
        <i class="fas fa-user text-white text-2xl"></i>
      </div>
      <h3 class="text-4xl font-bold text-black tracking-wide drop-shadow-lg">Professional Profile</h3>
    </div>
    <div class="backdrop-blur-xl bg-white/20 border border-white/40 rounded-2xl p-8">
      <p class="text-black text-lg leading-relaxed font-semibold drop-shadow-md">
        <%= profile?.about || 'Distinguished legal professional committed to excellence in client service and legal advocacy.' %>
      </p>
    </div>
  </div>

  <!-- Premium Professional Metrics -->
  <div class="backdrop-blur-xl bg-white/15 border border-white/30 rounded-3xl shadow-2xl mb-12 p-10">
    <div class="flex items-center mb-10">
      <div class="w-16 h-16 bg-gradient-to-br from-purple-500 to-indigo-600 rounded-2xl flex items-center justify-center mr-6 shadow-xl">
        <i class="fas fa-chart-bar text-white text-2xl"></i>
      </div>
      <% if (profile?.role === 'lawyer') { %>
        <h3 class="text-4xl font-bold text-black tracking-wide drop-shadow-lg">Professional Excellence</h3>
      <% } else { %>
        <h3 class="text-4xl font-bold text-black tracking-wide drop-shadow-lg">Community Leadership</h3>
      <% } %>
    </div>
    <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
      <% if (profile?.role === 'lawyer') { %>
        <div class="group text-center p-8 backdrop-blur-xl bg-gradient-to-br from-blue-500/30 to-blue-600/40 border border-blue-400/50 rounded-3xl hover:scale-105 transition-all duration-300 cursor-pointer">
          <div class="text-5xl font-bold text-blue-100 mb-4 drop-shadow-lg"><%= profile?.posts?.length || 0 %></div>
          <div class="text-lg font-bold text-black mb-2 drop-shadow-md">Legal Publications</div>
          <i class="fas fa-file-alt text-blue-200 text-3xl group-hover:scale-110 transition-transform drop-shadow-md"></i>
        </div>
        <div class="group text-center p-8 backdrop-blur-xl bg-gradient-to-br from-emerald-500/30 to-emerald-600/40 border border-emerald-400/50 rounded-3xl hover:scale-105 transition-all duration-300 cursor-pointer">
          <div class="text-5xl font-bold text-emerald-100 mb-4 drop-shadow-lg"><%= connectionsCount %></div>
          <div class="text-lg font-bold text-black mb-2 drop-shadow-md">Elite Network</div>
          <i class="fas fa-users text-emerald-200 text-3xl group-hover:scale-110 transition-transform drop-shadow-md"></i>
        </div>
        <div class="group text-center p-8 backdrop-blur-xl bg-gradient-to-br from-purple-500/30 to-purple-600/40 border border-purple-400/50 rounded-3xl hover:scale-105 transition-all duration-300 cursor-pointer">
          <div class="text-5xl font-bold text-purple-100 mb-4 drop-shadow-lg"><%= profile?.lawyer?.Educations?.length || 0 %></div>
          <div class="text-lg font-bold text-black mb-2 drop-shadow-md">Advanced Degrees</div>
          <i class="fas fa-graduation-cap text-purple-200 text-3xl group-hover:scale-110 transition-transform drop-shadow-md"></i>
        </div>
        <div class="group text-center p-8 backdrop-blur-xl bg-gradient-to-br from-amber-500/30 to-amber-600/40 border border-amber-400/50 rounded-3xl hover:scale-105 transition-all duration-300 cursor-pointer">
          <div class="text-5xl font-bold text-amber-100 mb-4 drop-shadow-lg"><%= new Date().getFullYear() - new Date(profile?.createdAt || new Date()).getFullYear() %></div>
          <div class="text-lg font-bold text-black mb-2 drop-shadow-md">Years of Excellence</div>
          <i class="fas fa-calendar-alt text-amber-200 text-3xl group-hover:scale-110 transition-transform drop-shadow-md"></i>
        </div>
      <% } else { %>
        <div class="group text-center p-8 backdrop-blur-xl bg-gradient-to-br from-blue-500/30 to-blue-600/40 border border-blue-400/50 rounded-3xl hover:scale-105 transition-all duration-300 cursor-pointer">
          <div class="text-5xl font-bold text-blue-100 mb-4 drop-shadow-lg"><%= profile?.posts?.length || 0 %></div>
          <div class="text-lg font-bold text-black mb-2 drop-shadow-md">Forum Contributions</div>
          <i class="fas fa-comments text-blue-200 text-3xl group-hover:scale-110 transition-transform drop-shadow-md"></i>
        </div>
        <div class="group text-center p-8 backdrop-blur-xl bg-gradient-to-br from-emerald-500/30 to-emerald-600/40 border border-emerald-400/50 rounded-3xl hover:scale-105 transition-all duration-300 cursor-pointer">
          <div class="text-5xl font-bold text-emerald-100 mb-4 drop-shadow-lg"><%= connectionsCount %></div>
          <div class="text-lg font-bold text-black mb-2 drop-shadow-md">Active Network</div>
          <i class="fas fa-users text-emerald-200 text-3xl group-hover:scale-110 transition-transform drop-shadow-md"></i>
        </div>
        <div class="group text-center p-8 backdrop-blur-xl bg-gradient-to-br from-purple-500/30 to-purple-600/40 border border-purple-400/50 rounded-3xl hover:scale-105 transition-all duration-300 cursor-pointer">
          <div class="text-5xl font-bold text-purple-100 mb-4 drop-shadow-lg"><%= connectionsCount %></div>
          <div class="text-lg font-bold text-black mb-2 drop-shadow-md">Legal Connections</div>
          <i class="fas fa-balance-scale text-purple-200 text-3xl group-hover:scale-110 transition-transform drop-shadow-md"></i>
        </div>
        <div class="group text-center p-8 backdrop-blur-xl bg-gradient-to-br from-amber-500/30 to-amber-600/40 border border-amber-400/50 rounded-3xl hover:scale-105 transition-all duration-300 cursor-pointer">
          <div class="text-5xl font-bold text-amber-100 mb-4 drop-shadow-lg"><%= Math.floor((new Date() - new Date(profile?.createdAt || new Date())) / (1000 * 60 * 60 * 24)) %></div>
          <div class="text-lg font-bold text-black mb-2 drop-shadow-md">Days Active</div>
          <i class="fas fa-calendar-check text-amber-200 text-3xl group-hover:scale-110 transition-transform drop-shadow-md"></i>
        </div>
      <% } %>
    </div>
  </div>

  <!-- Premium Expertise & Interests -->
  <div class="backdrop-blur-xl bg-white/15 border border-white/30 rounded-3xl shadow-2xl mb-12 p-10">
    <div class="flex items-center mb-10">
      <% if (profile?.role === 'lawyer') { %>
        <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-2xl flex items-center justify-center mr-6 shadow-xl">
          <i class="fas fa-gavel text-white text-2xl"></i>
        </div>
        <h3 class="text-4xl font-bold text-black tracking-wide drop-shadow-lg">Legal Expertise</h3>
      <% } else { %>
        <div class="w-16 h-16 bg-gradient-to-br from-pink-500 to-rose-600 rounded-2xl flex items-center justify-center mr-6 shadow-xl">
          <i class="fas fa-heart text-white text-2xl"></i>
        </div>
        <h3 class="text-4xl font-bold text-black tracking-wide drop-shadow-lg">Interests & Legal Areas</h3>
      <% } %>
    </div>
    <div class="flex flex-wrap gap-4">
      <% if (profile?.role === 'lawyer') { %>
        <% if (profile?.lawyer?.legalAreas) { %>
          <% profile.lawyer.legalAreas.split(',').forEach(area => { %>
            <span class="group px-6 py-3 backdrop-blur-xl bg-gradient-to-r from-blue-500/40 to-indigo-600/50 border border-blue-400/60 text-black rounded-2xl font-bold text-lg hover:scale-105 transition-all duration-300 cursor-pointer drop-shadow-lg">
              <i class="fas fa-gavel mr-2 group-hover:rotate-12 transition-transform"></i><%= area.trim() %>
            </span>
          <% }); %>
        <% } else { %>
          <div class="backdrop-blur-xl bg-white/20 border border-white/40 rounded-2xl p-6">
            <p class="text-black text-lg font-semibold drop-shadow-md">No legal areas specified yet. <a href="/in/edit" class="text-blue-600 hover:text-blue-800 font-bold underline">Add your expertise areas</a></p>
          </div>
        <% } %>
      <% } else { %>
        <% if (profile?.interests) { %>
          <% profile.interests.split(',').forEach(interest => { %>
            <span class="group px-6 py-3 backdrop-blur-xl bg-gradient-to-r from-pink-500/40 to-rose-600/50 border border-pink-400/60 text-black rounded-2xl font-bold text-lg hover:scale-105 transition-all duration-300 cursor-pointer drop-shadow-lg">
              <i class="fas fa-star mr-2 group-hover:rotate-12 transition-transform"></i><%= interest.trim() %>
            </span>
          <% }); %>
        <% } else { %>
          <div class="backdrop-blur-xl bg-white/20 border border-white/40 rounded-2xl p-6">
            <p class="text-black text-lg font-semibold drop-shadow-md">No interests specified yet. <a href="/in/edit" class="text-pink-600 hover:text-pink-800 font-bold underline">Add your interests</a></p>
          </div>
        <% } %>
      <% } %>
    </div>
    
    <!-- Additional Interests for Lawyers -->
    <% if (profile?.role === 'lawyer' && profile?.interests) { %>
      <div class="mt-10">
        <h4 class="text-2xl font-bold text-white mb-6 flex items-center drop-shadow-lg">
          <div class="w-12 h-12 bg-gradient-to-br from-emerald-500 to-green-600 rounded-xl flex items-center justify-center mr-4 shadow-xl">
            <i class="fas fa-heart text-white"></i>
          </div>
          Personal Interests
        </h4>
        <div class="flex flex-wrap gap-4">
          <% profile.interests.split(',').forEach(interest => { %>
            <span class="group px-6 py-3 backdrop-blur-xl bg-gradient-to-r from-emerald-500/40 to-green-600/50 border border-emerald-400/60 text-white rounded-2xl font-bold text-lg hover:scale-105 transition-all duration-300 cursor-pointer drop-shadow-lg">
              <i class="fas fa-heart mr-2 group-hover:scale-110 transition-transform"></i><%= interest.trim() %>
            </span>
          <% }); %>
        </div>
      </div>
    <% } %>
  </div>

  <!-- Contact Information -->
  <% if (profile?.role === 'lawyer') { %>
    <div class="bg-white rounded-2xl shadow-lg mb-8 p-8">
      <div class="flex items-center mb-6">
        <i class="fas fa-address-card text-blue-500 text-xl mr-3"></i>
        <h3 class="text-2xl font-bold text-gray-800">Professional Contact</h3>
      </div>
      <div class="space-y-4">
        <div class="flex items-center p-4 bg-gray-50 rounded-xl">
          <i class="fas fa-envelope text-blue-500 text-lg mr-4"></i>
          <div>
            <div class="font-medium text-gray-800">Professional Email</div>
            <div class="text-gray-600"><%= profile?.email || 'Not provided' %></div>
          </div>
        </div>
        <% if (profile?.city || profile?.country) { %>
          <div class="flex items-center p-4 bg-gray-50 rounded-xl">
            <i class="fas fa-map-marker-alt text-blue-500 text-lg mr-4"></i>
            <div>
              <div class="font-medium text-gray-800">Practice Location</div>
              <div class="text-gray-600">
                <% if (profile?.city && profile?.country) { %>
                  <%= profile.city %>, <%= profile.country %>
                <% } else if (profile?.city) { %>
                  <%= profile.city %>
                <% } else if (profile?.country) { %>
                  <%= profile.country %>
                <% } %>
              </div>
            </div>
          </div>
        <% } %>
        <% if (profile?.lawyer?.badgeNumber) { %>
          <div class="flex items-center p-4 bg-gray-50 rounded-xl">
            <i class="fas fa-id-badge text-blue-500 text-lg mr-4"></i>
            <div>
              <div class="font-medium text-gray-800">Bar Registration Number</div>
              <div class="text-gray-600"><%= profile.lawyer.badgeNumber %></div>
            </div>
          </div>
        <% } %>
        <div class="flex items-center p-4 bg-blue-50 rounded-xl border-l-4 border-blue-500">
          <i class="fas fa-calendar-check text-blue-500 text-lg mr-4"></i>
          <div>
            <div class="font-medium text-gray-800">Available for Consultation</div>
            <div class="text-gray-600">Contact for legal advice and representation</div>
          </div>
        </div>
      </div>
    </div>
  <% } else { %>
    <div class="bg-white rounded-2xl shadow-lg mb-8 p-8">
      <div class="flex items-center mb-6">
        <i class="fas fa-user-circle text-blue-500 text-xl mr-3"></i>
        <h3 class="text-2xl font-bold text-gray-800">Member Information</h3>
      </div>
      <div class="space-y-4">
        <div class="flex items-center p-4 bg-gray-50 rounded-xl">
          <i class="fas fa-calendar-alt text-blue-500 text-lg mr-4"></i>
          <div>
            <div class="font-medium text-gray-800">Member Since</div>
            <div class="text-gray-600"><%= new Date(profile?.createdAt || new Date()).toLocaleDateString('en-US', { year: 'numeric', month: 'long' }) %></div>
          </div>
        </div>
        <% if (profile?.city || profile?.country) { %>
          <div class="flex items-center p-4 bg-gray-50 rounded-xl">
            <i class="fas fa-map-marker-alt text-blue-500 text-lg mr-4"></i>
            <div>
              <div class="font-medium text-gray-800">Location</div>
              <div class="text-gray-600">
                <% if (profile?.city && profile?.country) { %>
                  <%= profile.city %>, <%= profile.country %>
                <% } else if (profile?.city) { %>
                  <%= profile.city %>
                <% } else if (profile?.country) { %>
                  <%= profile.country %>
                <% } %>
              </div>
            </div>
          </div>
        <% } %>
        <div class="flex items-center p-4 bg-green-50 rounded-xl border-l-4 border-green-500">
          <i class="fas fa-comments text-green-500 text-lg mr-4"></i>
          <div>
            <div class="font-medium text-gray-800">Community Engagement</div>
            <div class="text-gray-600">Active in legal discussions and knowledge sharing</div>
          </div>
        </div>
      </div>
    </div>
  <% } %>

  <!-- Badge Information Section (For Lawyers) -->
  <% if (profile?.role === 'lawyer' && profile?.lawyer) { %>
    <div class="bg-white rounded-2xl shadow-lg mb-8 p-8">
      <div class="flex items-center mb-4">
        <i class="fas fa-certificate text-blue-500 text-xl mr-3"></i>
        <h3 class="text-2xl font-bold text-gray-800">Professional Credentials</h3>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <!-- Enhanced Badge Information Card -->
        <div class="bg-blue-50 rounded-xl p-5 border border-blue-200">
          <div class="flex items-center justify-between mb-4">
            <h4 class="font-bold text-lg text-gray-800 flex items-center">
              <i class="fas fa-id-badge text-blue-600 mr-2"></i>
              Legal Authority Badge
            </h4>
            <% if (profile?.lawyer?.verificationStatus === 'approved') { %>
              <div class="w-6 h-6 bg-emerald-500 rounded-full flex items-center justify-center">
                <i class="fas fa-check text-white text-xs"></i>
              </div>
            <% } %>
          </div>
          
          <div class="space-y-3">
            <div class="flex items-center justify-between py-2 px-3 bg-white rounded-lg border border-blue-100">
              <span class="text-gray-600 font-medium">Badge Number</span>
              <span class="font-bold text-gray-800 font-mono text-sm"><%= profile?.lawyer?.badgeNumber || 'N/A' %></span>
            </div>
            
            <div class="flex items-center justify-between py-2 px-3 bg-white rounded-lg border border-blue-100">
              <span class="text-gray-600 font-medium">Issue Date</span>
              <span class="font-semibold text-gray-800 text-sm"><%= profile?.lawyer?.badgeIssueDate ? new Date(profile?.lawyer?.badgeIssueDate).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) : 'N/A' %></span>
            </div>
            
            <div class="mt-4">
              <div class="text-gray-600 font-medium mb-2">Verification Status</div>
              <div class="flex items-center justify-center py-3 px-4 rounded-lg
                <% if (profile?.lawyer?.verificationStatus === 'approved') { %>
                  bg-emerald-500 text-white
                <% } else if (profile?.lawyer?.verificationStatus === 'pending') { %>
                  bg-amber-500 text-white
                <% } else if (profile?.lawyer?.verificationStatus === 'rejected') { %>
                  bg-red-500 text-white
                <% } else { %>
                  bg-gray-500 text-white
                <% } %>">
                <% if (profile?.lawyer?.verificationStatus === 'approved') { %>
                  <i class="fas fa-shield-check mr-2"></i>
                  <div class="text-center">
                    <div class="font-bold">VERIFIED LAWYER</div>
                    <div class="text-emerald-100 text-xs">Authorized to practice law</div>
                  </div>
                <% } else if (profile?.lawyer?.verificationStatus === 'pending') { %>
                  <i class="fas fa-hourglass-half mr-2"></i>
                  <div class="text-center">
                    <div class="font-bold">VERIFICATION PENDING</div>
                    <div class="text-amber-100 text-xs">Under review by authorities</div>
                  </div>
                <% } else if (profile?.lawyer?.verificationStatus === 'rejected') { %>
                  <i class="fas fa-exclamation-triangle mr-2"></i>
                  <div class="text-center">
                    <div class="font-bold">VERIFICATION FAILED</div>
                    <div class="text-red-100 text-xs">Unable to verify credentials</div>
                  </div>
                <% } else { %>
                  <i class="fas fa-question-circle mr-2"></i>
                  <div class="text-center">
                    <div class="font-bold">STATUS UNKNOWN</div>
                    <div class="text-gray-100 text-xs">Verification status unavailable</div>
                  </div>
                <% } %>
              </div>
            </div>
          </div>
        </div>
        <% if (profile?.lawyer?.lawyer_docs && profile?.lawyer?.lawyer_docs.length > 0) { %>
          <!-- Enhanced Document Card -->
          <div class="bg-purple-50 rounded-xl p-5 border border-purple-200">
            <div class="flex items-center mb-4">
              <div class="w-10 h-10 bg-purple-500 rounded-lg flex items-center justify-center mr-3">
                <i class="fas fa-file-certificate text-white"></i>
              </div>
              <h4 class="font-bold text-lg text-gray-800">Legal Documents</h4>
            </div>
            
            <div class="bg-white rounded-lg p-3 border border-purple-100 mb-4">
              <div class="flex items-center text-gray-600 mb-1">
                <i class="fas fa-file-pdf text-red-500 mr-2"></i>
                <span class="font-medium">Official Badge Certificate</span>
              </div>
              <div class="text-sm text-gray-500">Issued by Legal Authority</div>
            </div>
            
            <a href="<%= profile?.lawyer?.lawyer_docs[0]?.idPath %>" target="_blank" 
               class="flex items-center justify-center w-full py-3 px-4 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition">
              <i class="fas fa-external-link-alt mr-2"></i>
              <span class="font-semibold">View Official Document</span>
            </a>
          </div>
        <% } %>
      </div>
    </div>
  <% } %>

  <!-- Education Section (Lawyers Only) -->
  <% if (profile?.role === 'lawyer' && profile?.lawyer?.Educations?.length > 0) { %>
    <div class="bg-white rounded-2xl shadow-lg mb-8 p-6 border border-gray-200">
      <div class="flex items-center mb-6">
        <div class="w-10 h-10 bg-emerald-500 rounded-lg flex items-center justify-center mr-3">
          <i class="fas fa-graduation-cap text-white"></i>
        </div>
        <h3 class="text-2xl font-bold text-gray-800">Legal Education</h3>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <% profile?.lawyer?.Educations?.forEach((edu, index) => { %>
          <div class="bg-emerald-50 rounded-xl p-5 border border-emerald-200 hover:shadow-md transition">
            <div class="flex items-start justify-between mb-3">
              <div class="w-8 h-8 bg-emerald-500 rounded-lg flex items-center justify-center text-white font-bold text-sm">
                <%= index + 1 %>
              </div>
              <div class="text-emerald-600">
                <i class="fas fa-university"></i>
              </div>
            </div>
            
            <h4 class="font-bold text-lg text-gray-800 mb-2 leading-tight">
              <%= edu?.institution || edu?.university || edu?.college %>
            </h4>
            
            <div class="bg-white rounded-lg p-3 mb-3 border border-emerald-100">
              <p class="font-semibold text-gray-700">
                <%= edu?.degree %>
              </p>
              <% if (edu?.fieldOfStudy) { %>
                <p class="text-emerald-600 text-sm font-medium mt-1">
                  <%= edu?.fieldOfStudy %>
                </p>
              <% } %>
            </div>
            
            <div class="flex items-center text-gray-500 text-sm">
              <i class="fas fa-calendar-alt mr-2"></i>
              <span class="font-medium">
                <%= edu?.startYear %> - <%= edu?.endYear || 'Present' %>
              </span>
            </div>
          </div>
        <% }); %>
      </div>
    </div>
  <% } %>

      <!-- Recent Activity Section -->
      <div class="bg-white rounded-2xl shadow-lg mb-8 p-6 border border-gray-200">
        <div class="flex items-center mb-6">
          <div class="w-10 h-10 bg-indigo-500 rounded-lg flex items-center justify-center mr-3">
            <i class="fas fa-clock text-white"></i>
          </div>
          <h3 class="text-2xl font-bold text-gray-800">Recent Activity</h3>
        </div>
        <div class="space-y-3">
          <div class="flex items-center p-4 bg-blue-50 rounded-lg border border-blue-200 hover:shadow-md transition">
            <div class="w-10 h-10 bg-blue-500 rounded-lg flex items-center justify-center mr-3">
              <i class="fas fa-user-plus text-white"></i>
            </div>
            <div>
              <div class="font-medium text-gray-800">Joined the platform</div>
              <div class="text-sm text-gray-600"><%= new Date(profile?.createdAt || new Date()).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %></div>
            </div>
          </div>
          <% if (profile?.role === 'lawyer' && profile?.posts?.length > 0) { %>
            <div class="flex items-center p-4 bg-green-50 rounded-xl border-l-4 border-green-500">
              <i class="fas fa-edit text-green-500 mr-3"></i>
              <div>
                <div class="font-medium text-gray-800">Published <%= profile.posts.length %> legal article<%= profile.posts.length !== 1 ? 's' : '' %></div>
                <div class="text-sm text-gray-600">Latest: "<%= profile.posts[0]?.title || 'Untitled' %>"</div>
              </div>
            </div>
          <% } else if (profile?.role !== 'lawyer') { %>
            <div class="flex items-center p-4 bg-green-50 rounded-xl border-l-4 border-green-500">
              <i class="fas fa-book-open text-green-500 mr-3"></i>
              <div>
                <div class="font-medium text-gray-800">Exploring legal resources</div>
                <div class="text-sm text-gray-600">Learning about rights and legal processes</div>
              </div>
            </div>
          <% } %>
          <% if (connectionsCount > 0) { %>
            <div class="flex items-center p-4 bg-purple-50 rounded-xl border-l-4 border-purple-500">
              <i class="fas fa-handshake text-purple-500 mr-3"></i>
              <div>
                <div class="font-medium text-gray-800">Connected with <%= connectionsCount %> professional<%= connectionsCount !== 1 ? 's' : '' %></div>
                <div class="text-sm text-gray-600">Building a strong network</div>
              </div>
            </div>
          <% } %>
        </div>
      </div>

      <!-- Achievements Section -->
      <div class="bg-white rounded-2xl shadow-lg mb-8 p-8">
        <div class="flex items-center mb-6">
          <i class="fas fa-trophy text-blue-500 text-xl mr-3"></i>
          <h3 class="text-2xl font-bold text-gray-800">Achievements</h3>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          <div class="flex items-center p-4 bg-yellow-50 rounded-xl border border-yellow-200">
            <i class="fas fa-medal text-yellow-500 text-2xl mr-3"></i>
            <div>
              <div class="font-semibold text-gray-800">Profile Complete</div>
              <div class="text-sm text-gray-600">Added profile information</div>
            </div>
          </div>
          <% if (connectionsCount >= 5) { %>
            <div class="flex items-center p-4 bg-blue-50 rounded-xl border border-blue-200">
              <i class="fas fa-users text-blue-500 text-2xl mr-3"></i>
              <div>
                <div class="font-semibold text-gray-800">Networker</div>
                <div class="text-sm text-gray-600">5+ connections</div>
              </div>
            </div>
          <% } %>
          <% if (profile?.role === 'lawyer' && profile?.posts?.length >= 3) { %>
            <div class="flex items-center p-4 bg-green-50 rounded-xl border border-green-200">
              <i class="fas fa-pen text-green-500 text-2xl mr-3"></i>
              <div>
                <div class="font-semibold text-gray-800">Legal Content Creator</div>
                <div class="text-sm text-gray-600">3+ legal articles published</div>
              </div>
            </div>
          <% } else if (profile?.role !== 'lawyer' && connectionsCount >= 3) { %>
            <div class="flex items-center p-4 bg-green-50 rounded-xl border border-green-200">
              <i class="fas fa-handshake text-green-500 text-2xl mr-3"></i>
              <div>
                <div class="font-semibold text-gray-800">Community Connector</div>
                <div class="text-sm text-gray-600">Connected with legal professionals</div>
              </div>
            </div>
          <% } %>
          <% if (profile?.role === 'lawyer' && profile?.lawyer?.Educations?.length > 0) { %>
            <div class="flex items-center p-4 bg-purple-50 rounded-xl border border-purple-200">
              <i class="fas fa-graduation-cap text-purple-500 text-2xl mr-3"></i>
              <div>
                <div class="font-semibold text-gray-800">Educated Professional</div>
                <div class="text-sm text-gray-600">Legal education verified</div>
              </div>
            </div>
          <% } %>
          <% if (profile?.role === 'lawyer' && profile?.lawyer?.badgeNumber) { %>
            <div class="flex items-center p-4 bg-red-50 rounded-xl border border-red-200">
              <i class="fas fa-certificate text-red-500 text-2xl mr-3"></i>
              <div>
                <div class="font-semibold text-gray-800">Certified Lawyer</div>
                <div class="text-sm text-gray-600">Badge verified</div>
              </div>
            </div>
          <% } %>
          <% if (new Date().getFullYear() - new Date(profile?.createdAt || new Date()).getFullYear() >= 1) { %>
            <div class="flex items-center p-4 bg-indigo-50 rounded-xl border border-indigo-200">
              <i class="fas fa-calendar-check text-indigo-500 text-2xl mr-3"></i>
              <div>
                <div class="font-semibold text-gray-800">Veteran Member</div>
                <div class="text-sm text-gray-600">1+ years active</div>
              </div>
            </div>
          <% } %>
        </div>
      </div>

      <!-- Posts Section (Lawyers Only) -->
      <% if (profile?.role === 'lawyer' && profile?.posts?.length > 0) { %>
        <div class="bg-white rounded-2xl shadow-lg mb-8 p-8">
          <div class="flex items-center mb-4">
            <i class="fas fa-newspaper text-blue-500 text-xl mr-3"></i>
            <h3 class="text-2xl font-bold text-gray-800">Recent Legal Articles</h3>
          </div>
          <div class="space-y-6">
            <% profile?.posts?.slice(0, 3).forEach(post=> { %>
              <div class="border border-blue-100 p-5 rounded-xl bg-blue-50 hover:shadow-md transition-shadow">
                <div class="flex items-start justify-between mb-2">
                  <h4 class="font-semibold text-lg text-blue-800">
                    <%= post?.title %>
                  </h4>
                  <span class="text-xs text-gray-500 bg-white px-2 py-1 rounded-full">
                    <%= new Date(post?.createdAt || new Date()).toLocaleDateString() %>
                  </span>
                </div>
                <p class="text-gray-700 mt-1 line-clamp-3">
                  <%= post?.content?.length > 150 ? post.content.substring(0, 150) + '...' : post?.content %>
                </p>
                <div class="mt-3 flex items-center text-sm text-gray-500">
                  <i class="fas fa-eye mr-1"></i>
                  <span class="mr-4">View full article</span>
                  <i class="fas fa-heart mr-1"></i>
                  <span>Reactions</span>
                </div>
              </div>
              <% }); %>
            <% if (profile?.posts?.length > 3) { %>
              <div class="text-center">
                <a href="/feed" class="inline-flex items-center px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition">
                  <i class="fas fa-plus mr-2"></i>
                  View All Articles (<%= profile.posts.length %>)
                </a>
              </div>
            <% } %>
          </div>
        </div>
      <% } else if (profile?.role !== 'lawyer') { %>
        <!-- Legal Resources Section (Visitors Only) -->
        <div class="bg-white rounded-2xl shadow-lg mb-8 p-8">
          <div class="flex items-center mb-6">
            <i class="fas fa-book text-blue-500 text-xl mr-3"></i>
            <h3 class="text-2xl font-bold text-gray-800">Legal Resources & Guides</h3>
          </div>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div class="border border-blue-100 p-5 rounded-xl bg-blue-50 hover:shadow-md transition-shadow">
              <div class="flex items-center mb-3">
                <i class="fas fa-home text-blue-600 text-lg mr-3"></i>
                <h4 class="font-semibold text-lg text-blue-800">Family Law Guide</h4>
              </div>
              <p class="text-gray-700 text-sm mb-3">Understanding your rights in family matters, divorce, and custody issues.</p>
              <a href="/resources/family-law" class="text-blue-600 hover:text-blue-800 text-sm font-medium">Learn More →</a>
            </div>
            <div class="border border-green-100 p-5 rounded-xl bg-green-50 hover:shadow-md transition-shadow">
              <div class="flex items-center mb-3">
                <i class="fas fa-briefcase text-green-600 text-lg mr-3"></i>
                <h4 class="font-semibold text-lg text-green-800">Employment Rights</h4>
              </div>
              <p class="text-gray-700 text-sm mb-3">Know your workplace rights, wage laws, and discrimination protections.</p>
              <a href="/resources/employment" class="text-green-600 hover:text-green-800 text-sm font-medium">Learn More →</a>
            </div>
            <div class="border border-purple-100 p-5 rounded-xl bg-purple-50 hover:shadow-md transition-shadow">
              <div class="flex items-center mb-3">
                <i class="fas fa-shield-alt text-purple-600 text-lg mr-3"></i>
                <h4 class="font-semibold text-lg text-purple-800">Consumer Protection</h4>
              </div>
              <p class="text-gray-700 text-sm mb-3">Protect yourself from fraud, understand contracts, and know your consumer rights.</p>
              <a href="/resources/consumer" class="text-purple-600 hover:text-purple-800 text-sm font-medium">Learn More →</a>
            </div>
            <div class="border border-orange-100 p-5 rounded-xl bg-orange-50 hover:shadow-md transition-shadow">
              <div class="flex items-center mb-3">
                <i class="fas fa-question-circle text-orange-600 text-lg mr-3"></i>
                <h4 class="font-semibold text-lg text-orange-800">Legal Q&A</h4>
              </div>
              <p class="text-gray-700 text-sm mb-3">Browse common legal questions and find answers from verified lawyers.</p>
              <a href="/feed" class="text-orange-600 hover:text-orange-800 text-sm font-medium">Browse Q&A →</a>
            </div>
          </div>
        </div>
      <% } %>

          <!-- Logout Button -->
          <% if (locals.loggedInUserId===profile?.id) { %>
            <div class="mt-8 text-center">
              <form action="/user/logout" method="get">
                <button type="submit"
                  class="px-6 py-2 border border-gray-400 rounded-full bg-gray-50 hover:bg-gray-100 shadow">Sign
                  Out</button>
              </form>
            </div>
            <% } %>
              <!-- Lawyer Feedback Section -->
              <% if (profile?.role==='lawyer' ) { %>
                <div class="bg-white rounded-2xl shadow-lg mb-8 p-8">
                  <div class="flex items-center mb-6">
                    <i class="fas fa-comments text-blue-500 text-xl mr-3"></i>
                    <h3 class="text-2xl font-bold text-gray-800">Client Feedback</h3>
                  </div>
                  <div id="feedback-list" class="space-y-0">
                    <!-- Feedback will be loaded here by JavaScript -->
                    <div id="feedback-spinner" class="flex justify-center items-center py-8">
                      <i class="fas fa-spinner fa-spin text-blue-500 text-3xl"></i>
                      <p class="ml-3 text-gray-500 italic">Loading feedback...</p>
                    </div>
                  </div>
                  <div class="mt-8 bg-gray-50 p-6 rounded-lg shadow-inner">
                    <h4 class="text-xl font-bold text-gray-800 mb-3">Summary</h4>
                    <div class="flex justify-between items-center">
                      <p class="text-lg font-semibold text-gray-700">Average Rating: <span id="average-rating"
                          class="text-blue-600 font-bold">N/A</span></p>
                      <p class="text-md text-gray-600">Total Reviews: <span id="total-reviews"
                          class="font-semibold">0</span></p>
                    </div>
                  </div>
                </div>

                <script>
                  document.addEventListener('DOMContentLoaded', async () => {
                    const lawyerId = '<%= profile.id %>'; // This is the User ID of the lawyer
                    const feedbackList = document.getElementById('feedback-list');
                    const averageRatingEl = document.getElementById('average-rating');
                    const totalReviewsEl = document.getElementById('total-reviews');
                    const feedbackSpinner = document.getElementById('feedback-spinner');

                    if (!lawyerId || !feedbackList || !feedbackSpinner) {
                      console.error('Required DOM elements for feedback not found.');
                      if (feedbackList) feedbackList.innerHTML = '<p class="text-red-500">Client-side error: Could not initialize feedback section.</p>';
                      if (feedbackSpinner) feedbackSpinner.style.display = 'none';
                      return;
                    }

                    try {
                      const response = await fetch(`/feedback/lawyer/${lawyerId}`);
                      if (feedbackSpinner) feedbackSpinner.style.display = 'none'; // Hide spinner once response is received

                      if (!response.ok) {
                        let errorText = '';
                        try {
                          errorText = await response.text(); // Attempt to get more error details
                        } catch (e) {
                          // If response.text() fails, use a generic message
                          errorText = 'Could not retrieve detailed error from server.';
                        }
                        feedbackList.innerHTML = `<p class="text-red-500 text-center py-4 font-semibold">Could not load feedback. Server responded with ${response.status}.</p><p class="text-gray-600 text-xs text-center">${errorText || ''}</p>`;
                        console.error('Failed to fetch feedback:', response.status, response.statusText, errorText);
                        return;
                      }
                      const data = await response.json();

                      averageRatingEl.textContent = data.averageRating > 0 ? `${data.averageRating} / 5` : 'No ratings yet';
                      totalReviewsEl.textContent = data.totalReviews;

                      if (data.feedback && data.feedback.length > 0) {
                        feedbackList.innerHTML = ''; // Clear loading/error message
                        data.feedback.forEach(fb => {
                          const reviewerName = fb.User && fb.User.firstName && fb.User.lastName ? `${fb.User.firstName} ${fb.User.lastName}` : 'Anonymous User';
                          const feedbackElement = `
                <div class="border border-gray-200 p-6 rounded-xl bg-white shadow-lg hover:shadow-xl transition-shadow duration-300 ease-in-out mb-4">
                  <div class="flex items-center justify-between mb-3">
                    <strong class="text-indigo-600 text-lg font-semibold">${reviewerName}</strong>
                    <div class="text-yellow-400 flex items-center space-x-1">
                      ${[...Array(5)].map((_, i) => `<i class="fas fa-star ${i < fb.rating ? 'text-yellow-400' : 'text-gray-300'} text-md"></i>`).join('')}
                    </div>
                  </div>
                  <p class="text-gray-500 italic text-xs mb-3">Reviewed on: ${new Date(fb.created_at).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}</p>
                  <p class="text-gray-700 text-md leading-relaxed">${fb.review || 'No comment provided.'}</p>
                </div>
              `;
                          feedbackList.insertAdjacentHTML('beforeend', feedbackElement);
                        });
                      } else {
                        feedbackList.innerHTML = '<p class="text-gray-500 italic text-center py-6">This lawyer has not received any feedback yet.</p>';
                      }
                    } catch (error) {
                      if (feedbackSpinner) feedbackSpinner.style.display = 'none';
                      feedbackList.innerHTML = '<p class="text-red-600 font-semibold text-center py-4">An unexpected error occurred while loading feedback. Please check the browser console for more details.</p>';
                      console.error('Error fetching or processing feedback:', error);
                    }
                  });
                </script>
                <% } %>

<script>
  async function uploadProfileImage(input) {
    if (input.files && input.files[0]) {
      const file = input.files[0];
      
      // Validate file type
      if (!file.type.startsWith('image/')) {
        alert('Please select an image file.');
        return;
      }
      
      // Validate file size (5MB limit)
      if (file.size > 5 * 1024 * 1024) {
        alert('File size must be less than 5MB.');
        return;
      }
      
      const formData = new FormData();
      formData.append('profileImage', file);
      
      try {
        // Show loading state
        const profileImg = document.getElementById('profileImage');
        const originalSrc = profileImg.src;
        
        const response = await fetch('/in/upload-profile-image', {
          method: 'POST',
          body: formData
        });
        
        const result = await response.json();
        
        if (result.success) {
          // Update the profile image
          profileImg.src = result.imagePath;
          alert('Profile image updated successfully!');
        } else {
          alert('Failed to upload image: ' + (result.error || 'Unknown error'));
          profileImg.src = originalSrc;
        }
      } catch (error) {
        console.error('Error uploading profile image:', error);
        alert('Failed to upload image. Please try again.');
      }
    }
  }
</script>
                  <!-- Font Awesome CDN for icons -->
                  <link rel="stylesheet"
                    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />